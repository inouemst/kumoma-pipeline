# 第一段階 完了報告（全面改訂版） — データ基盤

生成日時：2026年7月23日 / 対象：青空文庫 全作品
初回報告からの差し戻し対応（第1〜4便）を経た最終版。

---

## 0. 核心の問いへの答え

> **青空文庫には、「15分で読める口語の短編」が実際に何件あるのか。**

## **10,670件（対象17,456件の61.1%）**

この数字は3段階で動いた。

| 段階 | 件数 | 割合 |
|---|---|---|
| 初回報告（括弧の破綻・文語判定の欠陥あり） | 11,561件 | 66.2% |
| 第2便：文語判定を作り直した後 | 11,146件 | 63.8% |
| **第4便：読解速度モデルを適用した後（最終）** | **10,670件** | **61.1%** |

**下がったが、桁は変わっていない。** 旧字旧仮名・文語体は現代の口語小説と同じ速さで読めないという当たり前の事実を反映させた結果であり、この下降は正しい。依然として全体の6割が「15分・口語」の枠に入る。この数字が、雲間というプロダクトの存在意義を支える。

---

## 1. 全体の処理結果

| 項目 | 件数 | 割合 |
|---|---|---|
| person ページから抽出した一意な作品ID | 17,845件 | 100% |
| 正常に処理できた作品 | 17,458件 | 97.8% |
| うち対象外として index.json から除外 | 2件 | — |
| **最終的な収録作品数** | **17,456件** | **97.8%** |
| 対象外（本文取得不可・著作権存続等） | 227件 | 1.3% |
| その他エラー | 160件 | 0.9% |

`index.json` サイズ：**19.85 MB**（17,456作品）。

---

## 2. 第1〜4便で何が壊れていて、何を直したか（通史）

初回報告は数値上「成功率97.8%」を示していたが、レビューで3つの重大な欠陥が見つかり、4便に分けて対応した。

### 第1便：カギ括弧の深さ追跡が破綻していた

指示書が指定した「カギ括弧の深さを文書全体で追跡する」方式が誤りだった。日本語の組版では複数段落にまたがる会話は各段落の頭に「を繰り返すが」は最後にしか付かないため、対応が一度崩れると本文全体が「会話文の中」として汚染される。こころで実測：「707個に対し」652個、差55個。結果、はじまりの一文が113,161字（本文まるごと）になる暴走が発生していた。

**対応**：深さを行（段落）ごとにリセットする方式に変更。複数行にまたがる会話の一部を取りこぼす（過小評価）副作用は許容し、暴走（全文汚染）を防いだ。あわせて、はじまりの一文に150字の上限を設けた（UIはフェードアウト表示）。

### 第2便：文語体判定のrecallが不足していた

樋口一葉の全43作品（すべて文語）で検証したところ、文語と判定できたのはわずか28件（65%）。語尾リストが「けり・たり・なり・べし」等の終止形しか持たず、一葉の文体に頻出する連体形・已然形等の活用形を落としていた。

**対応**：実データ（一葉全43作＋森鴎外・泉鏡花・幸田露伴の文末頻度）から語尾リストを再設計し、樋口一葉recall 93%（40/43）、口語検証セット誤爆0%を達成する閾値（0.15）を、単一作品ではなく混同行列全体から決定した。

この過程で、**「旧字旧仮名だから文語」という前提そのものが誤りだった**ことが判明した。泉鏡花の代表作（高野聖・婦系図・歌行灯）は旧仮名で出版されているが実際は口語であり、幸田露伴も随筆は口語・物語（五重塔等）だけが文語だった。これは第一段階で発見した「芥川の新字旧仮名作品は口語」の逆方向の同じ罠であり、旧仮名メタデータと文語判定は独立した軸であるべきという設計方針が両方向で裏付けられた。

### 第3便：はじまりの一文への小見出し混入は「削除ではなく検品」で対応

見出しタグを伴わないプレーンテキストの小見出しが本文冒頭に残るケースが約1割見つかった。しかし除去ロジックを検討する過程で、**同じ「短い語＋全角スペース＋続き」という構造が、本物の見出し混入と、詩・児童書の意図的な字空けの両方に現れ、機械的に区別できない**ことが分かった（「小学教育の事」＝見出し／「あちらの　森の　ほうで」＝童話の正常な原文、が同じ形をしている）。

**対応**：自動除去は行わず、`はじまりの一文_要確認`フラグ（1,615件、9.3%）を立てるにとどめた。削除ロジックを完成させていたら、詩・童話の原文を破壊するリスクの方が大きいと判断した。

### 第4便：読解速度モデルの導入、対象外の確定、そして命名規則のバグ発見

読了時間の計算を、文字遣い・文語体に応じた速度モデル（下記4節）に変更。あわせて、本文取得不可の343件を検証する過程で、**このうち116件は著作権の問題ではなく、本文ファイルの命名規則の見落とし（`{作品ID}_{ファイルID}.html`だけでなく`{作品ID}.html`という別パターンが存在した）だったことが判明した**。

修正の結果、真に本文取得不可（著作権存続・外部リンク）なのは**227件**（343件ではない）。残る116件のうち大半は、既知の「旧式HTML形式」問題（前XHTML世代のファイル）に該当することも分かり、この問題の規模は当初把握していた37件よりずっと大きい**142件**だった（詳細は7節）。

**この発見は、初回報告時点の「343件＝著作権存続で確定」という結論が、1件（Alice6）の確認だけで343件全体に広げた**不十分な検証だったことを示している。今回、ランダム15件を実際に図書カードで開いて確認し、227件全件がリンク先を外部URL（例：`http://orion.mt.tama.hosei.ac.jp/...`）に持つ、真に著作権存続中の作品であることを検証した。

---

## 3. 対象外の確定

`index.json` から**229件**を対象外とした。

| 理由 | 件数 |
|---|---|
| 本文ファイルが取得できない（著作権存続・外部リンク、検証済み） | 227件 |
| 本文が実質存在しない（表題のみ／絵本で画像のみ） | 2件 |

本文なし2件の内訳：原民喜「渚」（作品ID 48490、表題のみで本文なし）、ケイト・グリーナウェイ「マリゴールド・ガーデン」（作品ID 54925、絵本で画像のみ）。

除外の記録は `errors.log` に理由付きで残し、`index.json` のトップレベルに `対象外件数` として件数・内訳を持たせた。握りつぶしていない。

---

## 4. 読解速度モデル

文字数は機械で取れる**事実**だが、読む速さは我々が置いた**仮定**である。この2つをデータ上はっきり分けた。

| 文字遣い | 基準速度（字/分） |
|---|---|
| 新字新仮名 | 600 |
| 新字旧仮名 | 450 |
| 旧字新仮名 | 420 |
| 旧字旧仮名 | 300 |

文語体と判定された作品は、上記の速度にさらに×0.7。各作品の計測値に `想定読解速度_字毎分` と `速度の根拠`（例：「旧字旧仮名・文語体」）を持たせ、どの規則が効いたか一件ごとに分かるようにした。係数は定数テーブルとして分離しており、後から差し替えられる。

**回帰確認**：たけくらべ（28,627字、旧字旧仮名・文語体、210字/分）→136分＝「それ以上」に正しく移動した。舞姫も同様。羅生門・こころは新字新仮名・口語のため600字/分のまま変化なし。

---

## 5. 読了時間区分の分布（速度モデル適用後）

| 区分 | 件数 | 割合 |
|---|---|---|
| 5分以内 | 6,666 | 38.2% |
| 15分以内（5分超〜15分） | 4,793 | 27.5% |
| 30分以内 | 2,818 | 16.1% |
| 1時間以内 | 1,748 | 10.0% |
| それ以上 | 1,431 | 8.2% |

短編（15分以内）は依然として**65.7%**を占める。速度モデル適用前（69.3%）から下がったが、これは文語・旧仮名作品が「実際にはもっと時間がかかる」という事実を反映した結果であり、正しい下降である。

---

## 6. 文語体・旧仮名・文字遣いの分布

| 項目 | 件数 | 割合 |
|---|---|---|
| 文語体 | 1,198 | 6.9% |
| 旧仮名（新字旧仮名＋旧字旧仮名＋旧字新仮名の一部） | 6,687 | 38.3% |

| 文字遣い区分 | 件数 | 割合 |
|---|---|---|
| 新字新仮名 | 10,650 | 61.0% |
| 新字旧仮名 | 4,528 | 25.9% |
| 旧字旧仮名 | 2,159 | 12.4% |
| 旧字新仮名 | 101 | 0.6% |
| その他 | 18 | 0.1% |

文語体はごく少数派（6.9%）だが、旧仮名は約4割。**「読みやすさ」の壁は文語かどうかより、仮名遣いの古さの方が広く効いている。** 入口③（読みやすさから）はこの実態に沿って設計するとよい。

---

## 7. クレジット欠損率と既知の限界

| 項目 | 欠損率 |
|---|---|
| 入力者 | 1.3% |
| 校正者 | 1.5% |
| 初出（元から記載が無いケースが大半） | 42.7% |

### 既知の限界（直せていない／直さないと決めたもの）

以下は失敗の記録ではなく、**第二段階が同じ地雷を踏まないための地図**である。

1. **会話文比率は「」内しか数えないため、戯曲（話者名＋セリフ形式）を捉えられない**。劇作家・岸田国士の作品でも会話文比率が構造的に低く出る（ただし全崩壊ではなく9%程度に限定）。
2. **文語判定は、句読点をほぼ使わない極端な擬古文を取りこぼす**（樋口一葉43作中3作。全角スペースのみ・読点のみで句点を持たない文体）。
3. **文語判定は、引用・エピグラフと地の文を区別できない**（古典文を引用する随筆が、地の文は口語でも比率が押し上げられる）。
4. **はじまりの一文に小見出しが混入している可能性がある作品が1,615件**（`はじまりの一文_要確認`フラグ付き。原文破壊のリスクを避けるため、意図的に自動除去していない）。
5. **旧式HTML形式（前XHTML世代、`<div class="main_text">`を持たない）が142件**（当初把握していた37件より多いことが第4便で判明）。今回は未対応。
6. **個別パースエラーが18件**（`bibliographical_information`が見当たらない等、要因不明の少数ケース）。
7. **作品IDの衝突が2件**（実語教・著作権法。それぞれ別ファイルで同じIDが使われている）。

---

## 8. 提出物

1. `index.json`（19.85MB、17,456作品） / `authors.json`（1,018著者） / `errors.log`
2. 処理スクリプト一式（`parse_persons.py` / `resolve_works.py` / `body_parser.py` / `build_index.py` / `finalize.py` / `self_check.py` / 検証用：`tune_bungo.py` / `diagnose_bungo.py`）
3. 本報告書、および第1〜4便の個別レポート（`FIX1_REPORT.md`〜`FIX4_REPORT.md`）

---

## 9. 第二段階への申し送り

- 「15分・口語」で61.1%がヒットするという結果は、速度モデルという現実的な仮定を通しても、入口②の設計をそのまま推し進めてよいことを裏付けている。
- 旧仮名が38.3%と多いため、入口③は文語/口語の二値だけでなく、旧仮名の有無を独立した軸として強調する価値がある。
- `はじまりの一文_要確認`フラグの1,615件は、案内文生成時に人手で見るか、機械的に一段見出しらしさを判定する別ロジックを検討する材料として使える。
- 著作権存続227件は雲間の対象外として確定。案内文・埋め込みの対象は17,456件。
- 旧式HTML形式142件・個別エラー18件・作品ID衝突2件は、対応する価値があるかどうかは第二段階の進捗次第で判断してよい規模（合計1%程度）。
- **速度モデルの係数（600/450/420/300、文語×0.7）は事実ではなく仮定である。** 第二段階以降、実際の読者フィードバックが得られたら差し替えることを前提に設計してある。
