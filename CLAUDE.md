# 雲間 — 案内文・ラベル生成プロジェクト

> このファイルはClaude Codeが毎セッション自動で読む指示書。
> ベース：`KUMOMA_HANDOVER_TO_CLAUDE_CODE.md`（2026-07-24付、claude.ai上のOpus作成）。
> 詳しい経緯・罠の記録・原則の理由はそちらの原文を参照すること。このファイルはその要約＋現在地。
> `RESEARCH_BRIEF.md`（2026-07-27追加）：プロジェクトの大義・競合状況（ブンゴウサーチ等）の整理。
> **第三段階（検索・UI設計）に着手する前に必ず読むこと**——差別化ポイントの前提が変わる内容を含む。

---

## 0. 三十秒で分かる現在地（2026-07-24 更新）

| 段階 | 内容 | 状態 |
|---|---|---|
| 第一段階 | 全作品の機械計測 | **完了**（17,456件、`index.json`） |
| 第二段階 第1便 | 案内文パイプライン構築、100篇実証 | 完了（済んでいるはずだが下記「現状のギャップ」参照） |
| 第二段階 第2便 | ラベル体系構築、100篇実証 | 同上 |
| **第二段階 第3便** | **16,203件の全件生成** | **← 今ここ。本番生成が始まっている（2026-07-26〜）** |

**現在の進捗（2026-07-26）：**
- 較正週・第2回ベンチマーク完了。N=15を本番バッチサイズとして確定
- 97件（`benchmark2_n15.json`）を本番採用し、`state/checkpoint.jsonl`に記録済み
- 公開サイト（`kumoma`リポジトリ、GitHub Pages: https://inouemst.github.io/kumoma/ ）に反映済み。案内文の帯・形式/読後感による絞り込み実装済み
- 優先順位リスト`ranking_top150.json`（青空文庫2022年通年アクセスランキング上位150件）を作成。**現状は2022年単年のみ。複数年の和集合を取る改善は未実施**
- `aozora_src/`に本文HTMLをsparse-checkoutでクローン済み（現時点で43著者分・約518MB。全16,203件をカバーするには随時ディレクトリを追加する必要がある）

**本番第1バッチ完了（2026-07-26）：** `next_batch_ids.json`の97件を生成、`production_batch1.json`に保存。
独立検証（`verify_production_batch1.py`）で97/97件がゲート通過・著者名漏出0件・ラベル語彙外0件を確認。
`state/checkpoint.jsonl`に追記し、公開サイトに反映済み。**累計194/16,203件。**

**このバッチで判明した申し送り事項：** ランキング上位には同一作品の異なる版（例：銀河鉄道の夜=456と43737、
高瀬舟=691と45245、やまなし=46605と472、藪の中=24454と179）が複数含まれていた。**表記違い統合（352組・
358件削減）のロジックはまだコード化されていない**ため、今回はそれぞれ独立に生成した（両方ともゲート通過済み
で内容としては問題ないが、統合すれば省けた重複作業）。次にこの手の重複に当たる前に、統合ロジックの実装を
検討すること。

**本番第2バッチ完了（2026-07-27）：** ランキング151〜300位（`ranking_151_300.json`）から未処理97件を選定・生成。
独立検証（`verify_production_batch2.py`）で97/97件がゲート通過・著者名漏出0件・ラベル語彙外0件を確認。
**累計291/16,203件。**

**新たに判明した罠：** 共著作品（例：アイヌ神謡集、ファウスト、茶の本）は、`index.json`の`著者ID`欄が
主たる著者のIDだけを指しており、実際の本文ファイルは**共著者側のディレクトリに置かれている場合がある**。
`著者ID`だけでsparse-checkoutの対象ディレクトリを決めると本文が見つからないことがあるため、
`本文URL`から実際のパスを直接読み取ること（次のバッチ以降、選定スクリプトでも考慮すべき点）。

**本番第3バッチ完了（2026-07-28）：** `next_batch_ids.json`の97件（第1・第2バッチと重複なし）をN=15で
7グループに分けて生成、`production_batch3.json`に保存。独立検証（`verify_production_batch3.py`）で
97/97件がゲート通過（100%）・著者名漏出0件・ラベル語彙外0件・文末多様性OKを確認。
`state/checkpoint.jsonl`に追記済み（388行）。**累計388/16,203件。**
- 共著作品の罠（みみずのたはこと=1704、著者ID「279、280」）は本文URLからディレクトリを特定して回避
- `aozora_src`のsparse-checkout対象に著者ディレクトリ`cards/000279`が未クローンだったため`git sparse-checkout add`で追加
- **新たに確立した運用ルール（作品名と同一文字列を含む固有名詞の扱い）：** 案内文が作品名と偶然同じ文字列を
  含むケースが12件あった（貝の火／赤いくつ／銀の匙／文鳥／風野又三郎／アグニの神／浦島太郎／ツェねずみ／
  ピアノ／十二月八日／おぎん／パンドラの匣）。これらはいずれも本文中に実在し、物語の中心をなす固有名詞・
  地名・日付であり、8章の「羅生門」の例（案内文に「羅生門」という地名を使うのは可、というのが既存の前提）
  と同じ構造。**作品名＝一般名詞（例：「秘密」「舞踏会」）の場合は言い換えて回避し、作品名＝物語に不可欠な
  固有名詞・日付の場合はそのまま使ってよい**、という判断基準で運用した
- 逆に、ARCHAIC_VOCAB（第四層）は本文に実在する語でも問答無用で弾かれる（例：「侍女」「車夫」「人力車」
  「書生」「乳母」）。本文の表記どおりに書いても機械的に弾かれるので、案内文側で言い換える必要がある
- ARCHAIC_VOCABは部分一致で判定されるため、無関係な語の一部として偶然出現することがある（例：「女郎花」
  という花の名前が「女郎」を含むために弾かれた）。個別の語彙追加はせず、案内文側で別の語に差し替えて対応

**本番第4バッチ完了（2026-07-28、同日2セット目）：** ランキング401〜500位（`ranking_401_500.json`）から
未処理97件を選定・生成。独立検証（`verify_production_batch4.py`）で97/97件がゲート通過（100%）・
著者名漏出0件・ラベル語彙外0件・文末多様性OK（最頻「ていく。」「ている。」各15/97=15.5%）を確認。
`state/checkpoint.jsonl`に追記（485行）。**累計485/16,203件。**
- 今回は生成エージェント自身にcheckpoint/CLAUDE.md編集・git操作をさせず、呼び出し側（Opus）が
  独立検証後にまとめて行う分担に変更（第3バッチでエージェントが自らcheckpoint/CLAUDE.mdを更新し、
  役割が混線した反省から）
- 旧字旧仮名作品での現代表記への引きずられ（與吉→与吉、太郎兵衞→太郎兵衛、共産黨宣言→共産党宣言、
  本文「大阪」なのに「大坂」と誤記）が複数件発生。個別の罠として暗記するのではなく、生成のたびに
  本文の実際の表記を確認する徹底が必要
- 地名を助詞なしで連結すると本文と不一致になる例（「伊豆三島」→本文は「伊豆の三島」）を発見。固有名詞を
  組み合わせる際は本文の助詞の有無も含めて表記を合わせること
- 1件、本文にない具体的なもの（「駕籠」、本文は「車もなし」）を書いてしまっていたケースをゲートでは
  なく人間による見直しで発見・修正。ゲートは実在しない固有名詞は弾けるが、本文と矛盾する一般的な
  事物の記述までは検出できないことの実例

**本番第5バッチ完了（2026-07-28、同日3セット目）：** アクセスランキング（2022年・2021年）がほぼ枯渇したため、
97件中78件をランキングから、残り19件は**作品ID昇順のフォールバック**で補充（`build_next_batch.py`に実装）。
独立検証（`verify_production_batch5.py`）で97/97件がゲート通過（100%）・著者名漏出0件・ラベル語彙外0件・
文末多様性OKを確認。`state/checkpoint.jsonl`に追記（582行）。**累計582/16,203件。**
- **人気ランキング上位は実質使い切った。** 2022年上位500件に2021年上位150件を加えても新規はわずか1件
  だった（同じ定番作品がどの年もほぼ同じ顔ぶれ）。これ以上ランキング年度を増やす作業は費用対効果が低いと
  判断し、以降は「ランキングにあるものを先に、尽きたら作品ID昇順」という運用に切り替えた
- 著者の雅号・ペンネーム自己言及の罠（山之口貘詩集の「貘」＝著者本人のペンネーム）を発見。作品名だけでなく
  著者の号・筆名が本文中に出てきても、それが実質的に著者を名指ししている場合は使わない
- 作者自身が自分の代表作を列挙する随筆（「羅生門の後に」等）は、作者名だけでなく作品タイトルの列挙も
  著者名漏出と同じ扱いで避ける必要がある

**第三段階（検索・UI）の方針を運営者と確認（2026-07-28）：** RESEARCH_BRIEF.mdが示唆する
「発見＋読書体験の統合」は、プロジェクトの大義（0章：読者を青空文庫へ送り返す。読む機能は持たない）と
矛盾するため**採用しない**。差別化は「発見（複合軸検索）」に一本化する。
- **隣から**：埋め込み不要。ラベル（形式・主題・読後感・時代の質感）の重なりをブラウザ内のJSで
  比較するだけで実装できる。追加費用ゼロ、すぐ着手可能
- **言葉から**：真の意味検索（embedding）は検索のたびにAPI費用が発生するため不採用。代わりに、
  閉じたラベル語彙（主題30語・読後感20語程度）に対する「よくある言葉→ラベル」の対応表を
  **事前に（購読内で）作成し、静的ファイルとしてサイトに埋め込む**方式を採用する。
  検索時のAPI呼び出しは発生しない、疑似的な意味検索

**本番第6バッチ完了（2026-07-29）：** 人気ランキングが尽きたため作品ID昇順で97件を選定（今回はたまたま
芥川竜之介の作品が集中）。**トークン削減策を3点導入して実施：**
1. 生成エージェントに全文データ（`batchN_fulltext_full.json`）を直接読ませず、`gate_check_batch.py`
   （新規作成）経由でのみ照合させる（合否と不在語リストだけを返す。全文はスクリプト内部でのみ使用）
2. `extract_hints_batch.py`（新規作成）で本文冒頭4,000字からjanomeで固有名詞候補を事前抽出し、
   `batchN_hints.json`として生成前に渡す（現代表記への引きずられによる手戻りを防ぐ狙い）
3. 案内文を60〜75字目安（上限100字ぎりぎりを狙わない）に指示

**結果（実測）：** 平均字数64.1字（狙い通り）、97/97件ゲート通過、独立検証も一致。
**しかしトークン消費は595,080——過去5バッチ（577k〜599k、第4バッチのみ304k）とほぼ同水準で、
明確な削減効果は確認できなかった。** 第4バッチが軽かった理由は文字数の短さそのものではなく、
別の要因（内容の扱いやすさ等）だった可能性が高い。**「短く書かせれば安くなる」という仮説は、
今回の実測では裏付けられなかった。** 次回以降、削減策の効果測定を継続し、効かないなら別の手
（ツール呼び出し回数の削減、エージェントの中間出力の簡潔化等）を検討する。

**本番第7バッチ完了（2026-08-01）：** 作品ID昇順で97件（芥川竜之介・太宰治ほか混在）を生成。
今回は追加で「gate_check_batch.pyを1件ずつでなくグループ単位でまとめて呼ぶ」「途中経過の報告を
簡潔にする」を指示した。結果：平均字数68.5字、97/97件ゲート通過（独立検証済み）。

**しかしトークン消費は642,808——過去最高で、削減策を積んだにもかかわらず悪化した。**
**2回連続でコスト削減策が効果を示さなかったことになる。** 現時点での見立て：
このプロジェクトが求める品質（旧字体の正確な保持・著者名や結末の漏出ゼロ・固有名詞の実在照合を
毎回全件通す等）を守ろうとすると、1件ごとに本文を読み込んで吟味する「思考そのもの」がコストの
大半を占めており、ファイル読み込み量やツール呼び出し回数のような周辺的な工夫では大きくは動かない
可能性が高い。**次に試すなら、品質基準そのものを緩めるか（非推奨）、97件という規模を疑うか
（バッチサイズを分けて実測比較する）、あるいは「これが適正コストである」と受け止めて予算計画
（週にどれだけ回せるか）を先に決める方向に切り替えるべきかもしれない。運営者と相談中。**
（重要な事故：エージェントが検証用の`batch7_fulltext_full.json`等を「中間ファイル」として誤って
削除したため、git履歴から復元して独立検証した。今後は生成物の削除指示から`fulltext_full`系は
明示的に除外すること）

**→ 運営者確認済み（2026-08-01）：コスト削減策の追求はやめ、「これが適正コスト」として
受け止める方針に切り替えた。** 週3〜4バッチ（1バッチ97件）のペースを目安とする。

**進捗と見通し（2026-08-01時点）：**
- 累計 776 / 16,203件、残り 15,427件（バッチ換算で約160回）
- 週3バッチ：約53週間（約12.4ヶ月）／週3.5バッチ：約46週間（約10.6ヶ月）／週4バッチ：約40週間（約9.3ヶ月）
- **急ぐプロジェクトではないため、この所要期間は許容範囲。** 「育つ庭」モデルのもと、公開サイトは
  完成を待たずに随時更新していく

**今後の運用方針：**
- `gate_check_batch.py`（全文を読ませず照合）・`extract_hints_batch.py`（固有名詞ヒント事前生成）は、
  コスト削減効果は実証できなかったが、**品質面（表記ミスの手戻り防止）では引き続き有用**なため使い続ける
- これ以上のコスト削減の試行錯誤（バッチサイズの変更実験等）は、明確な要望がない限り行わない
- 週内の実施回数は運営者の判断・都合に合わせる（毎日である必要はない）

**本番第8バッチ完了（2026-08-01）：** 作品ID昇順で97件（太宰治・龍膽寺雄ほか混在）を生成。
独立検証で97/97件ゲート通過・著者名漏出0件・ラベル語彙外0件を確認。トークン消費584,275
（第6・7バッチと同水準。コスト削減策の追求はやめた方針通り、以後この数値の追跡は必須としない）。
`state/checkpoint.jsonl`に追記（873行）。**累計873/16,203件。**
- 同一著者内の隣接作品同士で登場人物名を取り違えるミス（348「文七元結」に354「闇夜の梅」の
  「鳶頭」を誤って割り当て）を発見・修正。全文検索での相互チェックが有効だった一例
- 龍膽寺雄のようなモダニズム・扇情的内容の作品は、具体的な描写を避け抽象的な要約にとどめる
  という判断で対応（結末や過度に生々しい描写を避けるという既存原則の延長）

**次に着手予定の新機能（2026-08-01、運営者から提案）：「新着文庫」ページ**
- 青空文庫本家の新規公開作品一覧（`https://www.aozora.gr.jp/index_pages/whatsnew1.html`、
  「随時更新」「公開日・作品名・作者名・図書カード直リンク」形式、約50件・直近半年分）を
  定期的に取得し、雲間サイト上に新着一覧として表示する
- **雲間側の案内文生成とは独立した機能。** 新着作品は測定・案内文生成の対象にせず、
  青空文庫の図書カードへ直リンクするだけでよい（「育つ庭」が完成しても陳腐化しない、
  本家が更新され続ける限り意味を持ち続ける機能）
- 運営者からの提案の趣旨：**「本家の思想に触れられる」導線として機能させたい。**
  新着作品＝今まさにボランティアが入力・校正した成果であることを伝える一文を添える案を検討中
  （具体的な文言・デザインは未確定、運営者と相談の上で詰める）

### 第2回ベンチマーク（2026-07-25、プロンプト修正後・独立検証済み）

較正週で見つかった欠陥（著者名記載・固有名詞の現代化）への対策として、プロンプトに
「著者名・作品名を書かない」「本文の表記を絶対に変えない」「生成後にARCHAIC_VOCABと
照合し自己修正する」を追加し、97篇フルベンチマークをN=3/8/15で再実施した。
**エージェントの自己申告（全件通過）は鵜呑みにせず、`verify_benchmark2.py`でgates.pyを
直接実行して独立に再検証した。結果は以下の通り、自己申告と食い違う部分があった。**

| 項目 | 合格線 | N=3 | N=8 | N=15 |
|---|---|---|---|---|
| 第三層(固有名詞)通過率 | 95%以上 | **91.8%**(自己申告100%は誤り) | 100% | 100% |
| 固有名詞Jaccard(対 手書き版) | 0.6以上 | 0.281 | 0.291 | 0.334 |
| 字数規格 | 100% | 100% | 100% | 100% |
| 型一致率 | 90%以上 | 97.6% | 97.6% | 98.8% |
| 文末多様性 | 各20%未満 | OK | OK | OK |
| 総合ゲート通過率 | — | 91.8% | 100% | 100% |

**結論：**
- 著者名記載・固有名詞の現代化という「本物の欠陥」への対策は効いた。**N=8とN=15は固有名詞の実在チェックを100%達成**（較正週は全N未達だった）
- **N=3だけ今回は基準未達（91.8%）。** 前回の較正週はN=8が最下位だったが、今回はN=3が最下位——N依存の単調な関係はなく、サンプル間のばらつきの可能性がある
- **Jaccard(手書き版との一致度)は3パターンとも0.6を大きく下回ったまま(0.28〜0.33)。** 固有名詞の正確さ(実在確認)は大きく改善したのに一致度は改善しなかった。これは「別々の書き手が同じ素材から違う固有名詞を選ぶ」自然な差である可能性が高く、**この基準(0.6)自体が創作的な圧縮作業に適切かどうか、運営者の再検討が必要**
- 生成結果は`benchmark2_n3/8/15.json`、検証スクリプトは`verify_benchmark2.py`

**推奨：** 固有名詞の正確さ・型一致・文末多様性が良好なN=8またはN=15を本番候補とし、
Jaccard基準は「参考値として記録するが合否には使わない」方向で運営者に確認する。

**→ 運営者確認済み（2026-07-25）：本番のバッチサイズは N=15 に確定。** Jaccard基準は
合否判定には使わず、参考値として記録する運用とする。

### 較正週は実施済み（2026-07-25、詳細は `CALIBRATION_REPORT.md`）

クラウドルーティン(1回)で較正週を実施した。結論：**本番移行はまだ早い。**

- **Pass A（2,000字ラベル先行）：不採用確定。** 4軸すべて合格線未達
- **N∈{3,8,15}ベンチマーク：全滅。** ただし原因はバッチサイズではなく**プロンプト側の具体的な欠陥2種**と特定できた：
  1. 著者名・作品名を案内文に書いてしまう（本文に出てこないので第三層に弾かれる。36件中6件）
  2. 固有名詞の現代化・誤用が一部再発（`389`たけくらべの偽「吉原」など、CLAUDE.md既知の罠そのものが再発。36件中4件、全291生成中約1.4%）
  - 残り26件はゲート側の誤検知（言い換えを固有名詞と誤認識）で実害なし
- **`fulltext_100.json` は再生成済み。** 生成入力用（4,000字）と、`fulltext_100_full.json`（ゲート正解データ用・全文）に**分離した**。理由：ゲート照合の正解データまで4,000字に絞ると、境界直後に初出する正当な固有名詞まで誤って弾いてしまうため（`gates.py`自身の自己テストで発覚）。**gates.pyのロジックは変更していない。渡すデータを使い分けただけ**
- 軽量版リポジトリは不採用（本文がプレーンテキストでXHTML非互換）。本家を sparse-checkout で使用、100作品で約1.1GB
- スループット参考値：N=8/15で約130〜140件/時間。全16,203件で粗い試算1〜3ヶ月（引き継ぎ書の推定と大きく矛盾しない）

**次にやること：** 上記のプロンプト欠陥2点を直し、小規模（15〜20篇程度）で再テストしてから、97篇フルベンチマークに戻る。97篇×3パターンをいきなりやり直さない（コストが大きいため）。

### 実行環境について（2026-07-25 追記）

**クラウドルーティン（CCR）は、Proプランの利用枠とは別の「クレジット」残高を消費する。** ローカルのこのセッション（Claude Code CLI）は引き続きPro/Maxの共有利用枠を使う。**運営者の希望：基本はローカルの利用枠でやりくりし、クレジット消費はなるべく避ける。** 小規模な検証・作業はローカルセッションで行い、クラウドルーティンは「PCを離れる時にどうしても必要な場合」に限定する。

---

## 1. このプロジェクトの原則（すべての判断の土台）

- **上書きしない** — 本文には触れない。案内文は本文の代わりではなく、入口
- **序列をつけない** — 「傑作」「名作」「代表作」等の評価語を使わない
- **寄生しない** — Wikipedia・文学事典・既存の解説を参照しない。本文だけを見る
- **書けないものは、書けないと言う** — もっともらしい嘘より空欄の方がよい
- **測れないものは、測れないと言う** — 検証していないことを「検証済み」と書かない

最後の2つが最重要。都合のよい条件下でだけ動く検証を、動作の証明として報告しない。

---

## 2. 【最重要】お金の話 — 毎セッション最初に確認

Pro以上の購読に含まれ追加課金なしで使えるが、罠が一つある：

> `ANTHROPIC_API_KEY` 環境変数が設定されていると、購読ではなくAPI従量課金になる。

**セッション開始時に毎回 `/status` を実行し、サブスクリプション認証を目視確認すること。**
API キー経由になっていたら作業を止めて運営者に報告する。

利用枠はclaude.aiチャットと共有。枠を使い切ったらその日は終わり（自然な日次区切り）。

---

## 3. 完成済み成果物の扱い

| ファイル | 中身 | 扱い |
|---|---|---|
| `index.json` | 全17,456作品の計測値・はじまりの一文 | **読むだけ。絶対に変更しない** |
| `authors.json` | 著者1,018人の集約 | 読むだけ |
| `under200_ids.json` | 本文200字未満895件のID | 除外リストとして使う |
| `body_parser.py` / `build_index.py` / `finalize.py` | 第一段階のパイプラインコード | 復旧済み。再利用する |
| `gates.py` / `label_vocab.py` | 品質ゲート・ラベル語彙（第2便版） | 復旧済み・動作確認済み。再利用する |
| `drafts_batch1.json` | 手書き97篇の案内文（ベンチマーク対照群） | 復旧済み（2版あった。採用理由は上記参照） |
| `selection100.json` / `labels_pass1,2.json` | 100篇の選定・ラベル2回分 | 復旧済み |
| `fulltext_100.json` | 100篇の本文冒頭4,000字（**生成入力用**） | 較正週で再生成済み |
| `fulltext_100_full.json` | 100篇の本文全文（**ゲート正解データ用**） | 較正週で新規作成。第三層の照合はこちらを使う |
| `benchmark_n3/8/15.json` / `label_2000char_test.json` | 較正週の生成結果一式 | 参照用。次の再テストの比較対象にできる |
| `CALIBRATION_REPORT.md` | 較正週の詳細報告書 | 一次資料。要約は本ファイル冒頭 |

`index.json`の「計測値」「はじまりの一文」は第一段階の確定成果。第二段階では一切変更しない。

---

## 4. これまでに判明した罠（同じ穴に落ちないための地図）

### データ取得
- `--filter=blob:none` のまま全件処理しない。ネットワーク取得が毎回発生する。sparse-checkout（coneモード）＋ `--depth 1`
- 本文ファイル名は `{作品ID}_{ファイルID}.html` だが `{作品ID}.html` という別パターンも実在（見落とすと誤って取得不能扱いになる）
- 文字コードは Shift_JIS

### 本文パース
- 本文の終端は `<div class="bibliographical_information">`。**`bibliography_information`ではない**
- カギ括弧の深さは行ごとにリセットする（文書全体で追跡すると複数段落の会話で破綻する）

### 判定ロジック
- 旧仮名＝文語ではない（独立した軸）
- 会話文比率は戯曲では機能しない（「」を使わないため）
- 閾値を1作品だけで「検証済み」としない

### 案内文
- 固有名詞は本文の表記のまま（豐太郎／淺野／亜米利加、等。現代表記に直さない）
- 本文にないことを具体化しない（「わが國」を「日本」と書かない）
- 本文が短すぎると案内文は書けない → 200字未満は対象外
- **著者名・作品名を案内文に書かない（2026-07-25 較正週で決定）。** 生成時に著者名・作品名をプロンプトに渡しているが、本文自体には著者名が出てこないため、書くと第三層ゲートに必ず弾かれる。随筆・評論で「〜が回想する随筆」のように書きたくなるが、本文中の言い回しだけで表現する
- **既知の罠（吉原、豐太郎等）を個別に潰しても、同種の罠が別の固有名詞で再発しうる。** 較正週で「たけくらべ→偽の吉原」が対策後も再発し、「安兵衞→安兵衛」「ニイチェ→ニーチェ」という新たな現代化ミスも見つかった。個別事例の暗記ではなく「本文にない表記は絶対に使わない」という原則そのものを徹底する必要がある

### ゲート
- 自己申告に依存するゲートはゲートではない。機械抽出（janome）に一本化
- 誤検知を消すために語彙リストへ語を追加しない。除外は「形の性質」で行う
- 文末集計は表層4文字で数えない（活用形の違いを別物として数えると偏りが過小評価される）

---

## 5. ゲートの仕様（`gates.py` 再構築の指針）

| 層 | 内容 | 実装方針 |
|---|---|---|
| 第三層 | 固有名詞が本文に実在するか | janome形態素解析で固有名詞抽出＋数詞除外。自己申告は使わない |
| 第四層 | 非流通語（古語）混入なし | ブラックリスト方式 |
| 第五層 | 60〜100字／評価語なし／結末示唆なし | 機械チェック |

禁止表現（GUIDE_TEXT_SPEC.md 4-3）：
不朽の名作／珠玉の一篇／心温まる／考えさせられる／胸を打つ／色褪せない／今なお／傑作／必読／
〜な人におすすめ／読者に問いかける／深く突き刺さる／人間の本質／普遍的なテーマ

---

## 6. 確定している設計判断

### 生成対象は16,203件
```
全作品 17,456件 − 200字未満 895件 − 表記違い統合 358件 = 16,203件
```
統合条件：同一著者・同一題名・文字数±10%以内・文字遣いが互いに異なる352組。
文字遣いまで同じ23組は統合しない（別作品の可能性）。`index.json`のエントリ自体は統合しない。

### 入力は本文の先頭4,000字のみ
結末を見せない構造的対策（「書くな」ではなく「見せない」）。

### 案内文の型は2つ
| 対象 | 型 |
|---|---|
| 物語 | 誰が・どこで・何をする話か |
| 随筆・評論 | 何を手がかりに・何について、どう論じているか |

### 「育つ庭」モデル
全16,203件の完成は公開条件ではない。生成順序はアクセスランキング順（読まれる作品から）。
UI側の規律（申し送り事項）：
1. 案内文の有無で並べ替えない
2. 「準備中」ラベルを出さない
3. 案内文がない作品も同じ視覚的重みで表示する

---

## 7. 較正週（実施済み。結果は `CALIBRATION_REPORT.md`）

2026-07-25にクラウドルーティンで実施済み。結果の要約は0章参照。**再ベンチマークが次の課題。**

**次にやること（本番移行の前提条件）：**
1. プロンプトに以下を追加：
   - 「著者名・作品名を案内文に書かない」を明記
   - 「本文にない表記・現代化した表記は絶対に使わない」を、個別事例の暗記ではなく原則として徹底する言い回しに強化
2. **97篇まるごとではなく15〜20篇程度の小規模で**、上記の修正が効くか再テストする（ローカルセッションで、コストを抑えて）
3. 改善が確認できたら、97篇フルベンチマーク（N∈{3,8,15}、合格線は前回と同じ）に戻り、本番のNを確定する
4. 実測スループット確定 → 完了予定日を算出 → 運営者が第3便仕様書を改訂

**合格線（変更なし）**：固有名詞通過率95%以上／手書きとのJaccard0.6以上／字数規格100%／型一致90%以上／文末偏り各20%未満。
**約束ではなく測定で進む。全項目を満たす最大のNを採用。満たすNがなければプロンプト側の問題として報告する。**

---

## 8. 本番運用（較正が終わってから）

毎日の型：
1. `/status` で認証確認
2. `state/checkpoint.jsonl` を読み、優先順リストの未処理IDを取得
3. N件ずつ処理：本文先頭4,000字を読む→案内文＋4軸ラベルをJSON化→`gates.py`にかける
   →不合格なら修正（上限3回）→3回落ちたら`案内文なし理由`を確定して次へ
4. 1件ごとにcheckpointへ追記（クラッシュ安全）
5. 枠が尽きたら進捗サマリ（済/残/推定完了日）を出して終了

出力形式：
```json
{
  "作品ID": "127",
  "案内文": "夜、羅生門の下で雨宿りする男。...",
  "案内文なし理由": null,
  "形式": ["小説"],
  "主題": ["貧困", "死", "身分・階級"],
  "読後感": ["不穏", "張りつめた"],
  "時代の質感": ["平安"]
}
```

`案内文なし理由`を必ず使えるようにしておく。書かせない逃げ道がないと必ず何か書いてしまう。

## 人手検品（第六層）— 運営者の仕事
層化抽出で200〜400件。判定ははい／いいえ／判断保留の三択のみ。
厚く見る層：随筆・評論／文語・旧字旧仮名／長大な作品／ゲートを2回以上落ちて通った作品（全件）。

### 8-1. 新しいバッチを公開サイトに反映する手順（2026-07-26確立）

生成が終わり検品も済んだら、次の手順で公開する。

1. `kumoma-pipeline`（このリポジトリ）側で、`kumoma_prototype.html`の`const D=[...]`に
   新規分の案内文・形式・読後感をマージする（作品IDで突き合わせ、`w[16]`=案内文、
   `w[17]`=形式、`w[18]`=読後感。無ければ`null`のままにする＝「育つ庭」の実装そのもの）
2. `state/checkpoint.jsonl`に1件ごと追記する
3. 公開用リポジトリ`kumoma`を別フォルダにクローンし（例：`C:\Users\81908\Desktop\kumoma-site`）、
   更新した`kumoma_prototype.html`を`index.html`としてコピーして commit・push する
4. GitHub Pagesが自動でビルドする（`gh api repos/inouemst/kumoma/pages/builds/latest`で状況確認可）。
   数十秒〜1分で反映される
5. `kumoma-pipeline`側にも変更後の`kumoma_prototype.html`をcommit・pushしておく（一次情報源はこちら）

**`kumoma`（公開）と`kumoma-pipeline`（内部作業）は別リポジトリ。** 混同しないこと。

---

## 9. まだ決まっていないこと（勝手に決めない）

1. 実スループット — 参考値は出たが確定値ではない（較正週：N=8/15で約130〜140件/時間。要実測、ゲート再試行込みの実運用速度は未計測）
2. 読後感ラベルを残すか落とすか — 保留中
3. 「淡々」と「端正」の統合 — 検討中
4. カタカナ抽出器の構造的な誤検知（較正週3-2節C）— 語彙追加ではない解決策が見つかっていない
5. 本番の全16,203件クローン時のディスク使用量 — cone modeのsparse-checkoutは著者ディレクトリ単位でしか絞れず、100作品で1.1GBだった。全件だと数十GB規模になりうる。non-cone（パターン指定）での削減余地は未検証
6. コミット著者情報の不一致（stop hookが指摘）— git config変更は指示上禁止のため未対応。対応要否は運営者判断

**決着した項目（もう議論しない）**：Pass A採否（不採用確定）／軽量版リポジトリ採否（不採用確定）／運営者のプラン（Pro確認済み）／著者名・作品名の言及可否（不可、4章に明記済み）／**本番のバッチサイズ（N=15に確定、2026-07-25）**／Jaccard基準の扱い（合否判定に使わず参考値扱い）

---

## 10. 最後に

検証していないことを検証済みと言わない。破れなかったという報告より、破れ方を見つけた報告の方が価値がある。急がない。
