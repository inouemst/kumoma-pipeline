# 雲間 — 案内文・ラベル生成プロジェクト

> このファイルはClaude Codeが毎セッション自動で読む指示書。
> ベース：`docs/KUMOMA_HANDOVER_TO_CLAUDE_CODE.md`（2026-07-24付、claude.ai上のOpus作成）。
> 詳しい経緯・罠の記録・原則の理由はそちらの原文を参照すること。このファイルはその要約＋現在地。
> `docs/RESEARCH_BRIEF.md`（2026-07-27追加）：プロジェクトの大義・競合状況（ブンゴウサーチ等）の整理。
> 各仕様書・報告書は `docs/` フォルダにまとめてある（採用しなかった検討も含め記録として保持）。索引は `docs/README.md`。
> **第三段階（検索・UI設計）に着手する前に必ず読むこと**——差別化ポイントの前提が変わる内容を含む。

---

## 0. 三十秒で分かる現在地（2026-07-24 更新）

| 段階 | 内容 | 状態 |
|---|---|---|
| 第一段階 | 全作品の機械計測 | **完了**（17,456件、`index.json`） |
| 第二段階 第1便 | 案内文パイプライン構築、100篇実証 | 完了（済んでいるはずだが下記「現状のギャップ」参照） |
| 第二段階 第2便 | ラベル体系構築、100篇実証 | 同上 |
| **第二段階 第3便** | **16,203件の全件生成** | **← 今ここ。本番生成が始まっている（2026-07-26〜）** |

**現在の進捗（2026-07-26）：**
- 較正週・第2回ベンチマーク完了。N=15を本番バッチサイズとして確定
- 97件（`benchmark2_n15.json`）を本番採用し、`state/checkpoint.jsonl`に記録済み
- 公開サイト（`kumoma`リポジトリ、GitHub Pages: https://inouemst.github.io/kumoma/ ）に反映済み。案内文の帯・形式/読後感による絞り込み実装済み
- 優先順位リスト`ranking_top150.json`（青空文庫2022年通年アクセスランキング上位150件）を作成。**現状は2022年単年のみ。複数年の和集合を取る改善は未実施**
- `aozora_src/`に本文HTMLをsparse-checkoutでクローン済み（現時点で43著者分・約518MB。全16,203件をカバーするには随時ディレクトリを追加する必要がある）

**本番第1バッチ完了（2026-07-26）：** `next_batch_ids.json`の97件を生成、`production_batch1.json`に保存。
独立検証（`verify_production_batch1.py`）で97/97件がゲート通過・著者名漏出0件・ラベル語彙外0件を確認。
`state/checkpoint.jsonl`に追記し、公開サイトに反映済み。**累計194/16,203件。**

**このバッチで判明した申し送り事項：** ランキング上位には同一作品の異なる版（例：銀河鉄道の夜=456と43737、
高瀬舟=691と45245、やまなし=46605と472、藪の中=24454と179）が複数含まれていた。**表記違い統合（352組・
358件削減）のロジックはまだコード化されていない**ため、今回はそれぞれ独立に生成した（両方ともゲート通過済み
で内容としては問題ないが、統合すれば省けた重複作業）。次にこの手の重複に当たる前に、統合ロジックの実装を
検討すること。

**本番第2バッチ完了（2026-07-27）：** ランキング151〜300位（`ranking_151_300.json`）から未処理97件を選定・生成。
独立検証（`verify_production_batch2.py`）で97/97件がゲート通過・著者名漏出0件・ラベル語彙外0件を確認。
**累計291/16,203件。**

**新たに判明した罠：** 共著作品（例：アイヌ神謡集、ファウスト、茶の本）は、`index.json`の`著者ID`欄が
主たる著者のIDだけを指しており、実際の本文ファイルは**共著者側のディレクトリに置かれている場合がある**。
`著者ID`だけでsparse-checkoutの対象ディレクトリを決めると本文が見つからないことがあるため、
`本文URL`から実際のパスを直接読み取ること（次のバッチ以降、選定スクリプトでも考慮すべき点）。

**本番第3バッチ完了（2026-07-28）：** `next_batch_ids.json`の97件（第1・第2バッチと重複なし）をN=15で
7グループに分けて生成、`production_batch3.json`に保存。独立検証（`verify_production_batch3.py`）で
97/97件がゲート通過（100%）・著者名漏出0件・ラベル語彙外0件・文末多様性OKを確認。
`state/checkpoint.jsonl`に追記済み（388行）。**累計388/16,203件。**
- 共著作品の罠（みみずのたはこと=1704、著者ID「279、280」）は本文URLからディレクトリを特定して回避
- `aozora_src`のsparse-checkout対象に著者ディレクトリ`cards/000279`が未クローンだったため`git sparse-checkout add`で追加
- **新たに確立した運用ルール（作品名と同一文字列を含む固有名詞の扱い）：** 案内文が作品名と偶然同じ文字列を
  含むケースが12件あった（貝の火／赤いくつ／銀の匙／文鳥／風野又三郎／アグニの神／浦島太郎／ツェねずみ／
  ピアノ／十二月八日／おぎん／パンドラの匣）。これらはいずれも本文中に実在し、物語の中心をなす固有名詞・
  地名・日付であり、8章の「羅生門」の例（案内文に「羅生門」という地名を使うのは可、というのが既存の前提）
  と同じ構造。**作品名＝一般名詞（例：「秘密」「舞踏会」）の場合は言い換えて回避し、作品名＝物語に不可欠な
  固有名詞・日付の場合はそのまま使ってよい**、という判断基準で運用した
- 逆に、ARCHAIC_VOCAB（第四層）は本文に実在する語でも問答無用で弾かれる（例：「侍女」「車夫」「人力車」
  「書生」「乳母」）。本文の表記どおりに書いても機械的に弾かれるので、案内文側で言い換える必要がある
- ARCHAIC_VOCABは部分一致で判定されるため、無関係な語の一部として偶然出現することがある（例：「女郎花」
  という花の名前が「女郎」を含むために弾かれた）。個別の語彙追加はせず、案内文側で別の語に差し替えて対応

**本番第4バッチ完了（2026-07-28、同日2セット目）：** ランキング401〜500位（`ranking_401_500.json`）から
未処理97件を選定・生成。独立検証（`verify_production_batch4.py`）で97/97件がゲート通過（100%）・
著者名漏出0件・ラベル語彙外0件・文末多様性OK（最頻「ていく。」「ている。」各15/97=15.5%）を確認。
`state/checkpoint.jsonl`に追記（485行）。**累計485/16,203件。**
- 今回は生成エージェント自身にcheckpoint/CLAUDE.md編集・git操作をさせず、呼び出し側（Opus）が
  独立検証後にまとめて行う分担に変更（第3バッチでエージェントが自らcheckpoint/CLAUDE.mdを更新し、
  役割が混線した反省から）
- 旧字旧仮名作品での現代表記への引きずられ（與吉→与吉、太郎兵衞→太郎兵衛、共産黨宣言→共産党宣言、
  本文「大阪」なのに「大坂」と誤記）が複数件発生。個別の罠として暗記するのではなく、生成のたびに
  本文の実際の表記を確認する徹底が必要
- 地名を助詞なしで連結すると本文と不一致になる例（「伊豆三島」→本文は「伊豆の三島」）を発見。固有名詞を
  組み合わせる際は本文の助詞の有無も含めて表記を合わせること
- 1件、本文にない具体的なもの（「駕籠」、本文は「車もなし」）を書いてしまっていたケースをゲートでは
  なく人間による見直しで発見・修正。ゲートは実在しない固有名詞は弾けるが、本文と矛盾する一般的な
  事物の記述までは検出できないことの実例

**本番第5バッチ完了（2026-07-28、同日3セット目）：** アクセスランキング（2022年・2021年）がほぼ枯渇したため、
97件中78件をランキングから、残り19件は**作品ID昇順のフォールバック**で補充（`build_next_batch.py`に実装）。
独立検証（`verify_production_batch5.py`）で97/97件がゲート通過（100%）・著者名漏出0件・ラベル語彙外0件・
文末多様性OKを確認。`state/checkpoint.jsonl`に追記（582行）。**累計582/16,203件。**
- **人気ランキング上位は実質使い切った。** 2022年上位500件に2021年上位150件を加えても新規はわずか1件
  だった（同じ定番作品がどの年もほぼ同じ顔ぶれ）。これ以上ランキング年度を増やす作業は費用対効果が低いと
  判断し、以降は「ランキングにあるものを先に、尽きたら作品ID昇順」という運用に切り替えた
- 著者の雅号・ペンネーム自己言及の罠（山之口貘詩集の「貘」＝著者本人のペンネーム）を発見。作品名だけでなく
  著者の号・筆名が本文中に出てきても、それが実質的に著者を名指ししている場合は使わない
- 作者自身が自分の代表作を列挙する随筆（「羅生門の後に」等）は、作者名だけでなく作品タイトルの列挙も
  著者名漏出と同じ扱いで避ける必要がある

**第三段階（検索・UI）の方針を運営者と確認（2026-07-28）：** docs/RESEARCH_BRIEF.mdが示唆する
「発見＋読書体験の統合」は、プロジェクトの大義（0章：読者を青空文庫へ送り返す。読む機能は持たない）と
矛盾するため**採用しない**。差別化は「発見（複合軸検索）」に一本化する。
- **隣から**：埋め込み不要。ラベル（形式・主題・読後感・時代の質感）の重なりをブラウザ内のJSで
  比較するだけで実装できる。追加費用ゼロ、すぐ着手可能
- **言葉から**：真の意味検索（embedding）は検索のたびにAPI費用が発生するため不採用。代わりに、
  閉じたラベル語彙（主題30語・読後感20語程度）に対する「よくある言葉→ラベル」の対応表を
  **事前に（購読内で）作成し、静的ファイルとしてサイトに埋め込む**方式を採用する。
  検索時のAPI呼び出しは発生しない、疑似的な意味検索

**本番第6バッチ完了（2026-07-29）：** 人気ランキングが尽きたため作品ID昇順で97件を選定（今回はたまたま
芥川竜之介の作品が集中）。**トークン削減策を3点導入して実施：**
1. 生成エージェントに全文データ（`batchN_fulltext_full.json`）を直接読ませず、`gate_check_batch.py`
   （新規作成）経由でのみ照合させる（合否と不在語リストだけを返す。全文はスクリプト内部でのみ使用）
2. `extract_hints_batch.py`（新規作成）で本文冒頭4,000字からjanomeで固有名詞候補を事前抽出し、
   `batchN_hints.json`として生成前に渡す（現代表記への引きずられによる手戻りを防ぐ狙い）
3. 案内文を60〜75字目安（上限100字ぎりぎりを狙わない）に指示

**結果（実測）：** 平均字数64.1字（狙い通り）、97/97件ゲート通過、独立検証も一致。
**しかしトークン消費は595,080——過去5バッチ（577k〜599k、第4バッチのみ304k）とほぼ同水準で、
明確な削減効果は確認できなかった。** 第4バッチが軽かった理由は文字数の短さそのものではなく、
別の要因（内容の扱いやすさ等）だった可能性が高い。**「短く書かせれば安くなる」という仮説は、
今回の実測では裏付けられなかった。** 次回以降、削減策の効果測定を継続し、効かないなら別の手
（ツール呼び出し回数の削減、エージェントの中間出力の簡潔化等）を検討する。

**本番第7バッチ完了（2026-08-01）：** 作品ID昇順で97件（芥川竜之介・太宰治ほか混在）を生成。
今回は追加で「gate_check_batch.pyを1件ずつでなくグループ単位でまとめて呼ぶ」「途中経過の報告を
簡潔にする」を指示した。結果：平均字数68.5字、97/97件ゲート通過（独立検証済み）。

**しかしトークン消費は642,808——過去最高で、削減策を積んだにもかかわらず悪化した。**
**2回連続でコスト削減策が効果を示さなかったことになる。** 現時点での見立て：
このプロジェクトが求める品質（旧字体の正確な保持・著者名や結末の漏出ゼロ・固有名詞の実在照合を
毎回全件通す等）を守ろうとすると、1件ごとに本文を読み込んで吟味する「思考そのもの」がコストの
大半を占めており、ファイル読み込み量やツール呼び出し回数のような周辺的な工夫では大きくは動かない
可能性が高い。**次に試すなら、品質基準そのものを緩めるか（非推奨）、97件という規模を疑うか
（バッチサイズを分けて実測比較する）、あるいは「これが適正コストである」と受け止めて予算計画
（週にどれだけ回せるか）を先に決める方向に切り替えるべきかもしれない。運営者と相談中。**
（重要な事故：エージェントが検証用の`batch7_fulltext_full.json`等を「中間ファイル」として誤って
削除したため、git履歴から復元して独立検証した。今後は生成物の削除指示から`fulltext_full`系は
明示的に除外すること）

**→ 運営者確認済み（2026-08-01）：コスト削減策の追求はやめ、「これが適正コスト」として
受け止める方針に切り替えた。** 週3〜4バッチ（1バッチ97件）のペースを目安とする。

**進捗と見通し（2026-08-21時点）：**
- 累計 2,037 / 16,203件、残り 14,166件（バッチ換算で約146回）
- 週3バッチ：約49週間（約11.3ヶ月）／週3.5バッチ：約42週間（約9.6ヶ月）／週4バッチ：約37週間（約8.4ヶ月）
- **急ぐプロジェクトではないため、この所要期間は許容範囲。** 「育つ庭」モデルのもと、公開サイトは
  完成を待たずに随時更新していく

**今後の運用方針：**
- `gate_check_batch.py`（全文を読ませず照合）・`extract_hints_batch.py`（固有名詞ヒント事前生成）は、
  コスト削減効果は実証できなかったが、**品質面（表記ミスの手戻り防止）では引き続き有用**なため使い続ける
- これ以上のコスト削減の試行錯誤（バッチサイズの変更実験等）は、明確な要望がない限り行わない
- 週内の実施回数は運営者の判断・都合に合わせる（毎日である必要はない）

**本番第8バッチ完了（2026-08-01）：** 作品ID昇順で97件（太宰治・龍膽寺雄ほか混在）を生成。
独立検証で97/97件ゲート通過・著者名漏出0件・ラベル語彙外0件を確認。トークン消費584,275
（第6・7バッチと同水準。コスト削減策の追求はやめた方針通り、以後この数値の追跡は必須としない）。
`state/checkpoint.jsonl`に追記（873行）。**累計873/16,203件。**
- 同一著者内の隣接作品同士で登場人物名を取り違えるミス（348「文七元結」に354「闇夜の梅」の
  「鳶頭」を誤って割り当て）を発見・修正。全文検索での相互チェックが有効だった一例
- 龍膽寺雄のようなモダニズム・扇情的内容の作品は、具体的な描写を避け抽象的な要約にとどめる
  という判断で対応（結末や過度に生々しい描写を避けるという既存原則の延長）

**本番第9バッチ完了（2026-08-02）：** ランキングが完全に尽きたため作品ID昇順で97件（作品ID431〜577、
森鴎外・宮沢賢治・岡本綺堂・佐々木味津三の捕物帳シリーズほか混在）を生成。独立検証
（`verify_production_batch9.py`）で97/97件ゲート通過（100%）・著者名漏出0件・ラベル語彙外0件・
文末多様性OK・平均字数71.1字を確認。`state/checkpoint.jsonl`に追記（970行）。**累計970/16,203件。**
- 著者ディレクトリの見落とし（`000087`）で本文抽出が1件失敗する事故が発生。本文URLから必要な
  著者ディレクトリを洗い出す際、リストの目視比較でチェックを1件飛ばしていたのが原因。
  次回以降、必要ディレクトリの差分はPython側で集合演算（set差分）を取って機械的に確認し、
  目視比較に頼らないようにする
- 遊郭・岡場所を舞台にした捕物帳シリーズで、ARCHAIC_VOCAB（女中・小者・丁稚等）や
  現代表記への引きずられ（ロシア→ロシヤ、欧州→欧洲、刀傷→刀疵等）が複数件発生したが、
  いずれもgate_check_batch.pyでの自己検証段階で修正済み。独立検証でも語彙外・不在語0件を確認

**本番第10バッチ完了（2026-08-03）：** 作品ID昇順で97件（佐左木俊郎・佐々木味津三の「右門捕物帳」
シリーズ・森鴎外・水野仙子・新美南吉ほか混在）を生成。独立検証（`verify_production_batch10.py`）で
97/97件ゲート通過（100%）・著者名漏出0件・ラベル語彙外0件・文末多様性OK・平均字数80.7字を確認。
`state/checkpoint.jsonl`に追記（1,067行）。**累計1,067/16,203件。**
- 必要な著者ディレクトリの洗い出しを、前回（第9バッチ）の目視比較ミスの反省を踏まえてPython側の
  集合演算（set差分）に切り替えて実施。6ディレクトリ（`000112, 000116, 000118, 000124, 000126, 000134`）
  の不足を機械的に検出・追加でき、抽出は初回から97/97件成功（見落とし事故は再発しなかった）
- 初回ドラフトで17件がゲート不合格。原因は現代仮名遣いへの引きずられ（雌鶏→雌鷄、国見→國見）、
  外来語表記違い（ベルリン→伯林、ドイツ→獨逸、蘭語→オランダ語）、本文にない具体化（藪入り→お盆、
  空駕籠→もぬけのから）。いずれもgate_check_batch.pyでの自己検証段階で修正され、独立検証でも
  全件通過を確認
- 「右門捕物帳」シリーズ（18件）では、既存バッチ（第9便）の慣例を踏襲し、主人公・伝六・敬四郎らを
  固有名詞ではなく「同心」「子分」「商売敵の同心」等の一般名詞で表現する運用を継続（シリーズ内の
  登場人物名取り違え防止）
- 高瀬舟（692番）など、表記違い統合ロジック未実装のため既出作品と同題の別IDが今回も含まれていた
  （既知の申し送り事項どおり、統合は依然未着手）
- 田中英光「さようなら」（729番）など死を主題とする重い随筆、性的・扇情的な内容を含む作品（667番・
  708番等）は、評価語・結末示唆を避けつつ抽象的な要約にとどめる既存方針で対応

**本番第11バッチ完了（2026-08-03、同日2セット目）：** 作品ID昇順で97件（夏目漱石26件・海野十三15件・
島崎藤村8件・辻潤7件・石川啄木6件ほか混在）を生成。独立検証（`verify_production_batch11.py`）で
96/97件に案内文あり・ゲート通過96/96（100%）・著者名漏出0件・ラベル語彙外0件・文末多様性OK・
平均字数67.3字を確認。`state/checkpoint.jsonl`に追記（1,164行）。**累計1,164/16,203件。**
- 作品ID857「Tada-Dada of Alangri-Gloriban」は本文がほぼ英語の言葉遊びで構成されており、日本語の
  案内文として要約困難と判断し「案内文なし」を選択（もっともらしい嘘より空欄が良いという原則どおり）
- **今回から初回ドラフトの不合格件数を追跡開始**（前バッチとの手戻り傾向比較のため運営者と合意）：
  初回21件が不合格（96件中75件合格＝78.1%）、うち多くはjanomeが「距離」「満韓」「雨音」「本州」等の
  通常語を固有名詞候補として誤抽出したことによる第三層の誤検知。言い換えで解消し最終的に全件通過
- 必要な著者ディレクトリの洗い出しは前回に続き集合演算（set差分）で実施、7ディレクトリ
  （`000150, 000154, 000159, 000162, 000163, 000164, 000166`）の不足を機械的に検出・追加、
  見落とし事故なし
- 「女中」（作品841）「女郎花」（作品885）がARCHAIC_VOCABに抵触（後者は既知の部分一致誤検知パターン）
  → それぞれ「家に仕える女」「おみなえし」に言い換えて解消

**本番第12バッチ完了（2026-08-03、同日3セット目）：** 作品ID昇順で97件（岡本綺堂「半七捕物帳」らしき
シリーズ51件・夢野久作8件・武田麟太郎5件ほか混在）を生成。独立検証（`verify_production_batch12.py`）で
97/97件ゲート通過（100%）・著者名漏出0件・ラベル語彙外0件・文末多様性OK・平均字数75.1字を確認。
`state/checkpoint.jsonl`に追記（1,261行）。**累計1,261/16,203件。**
- 初回ドラフトの不合格件数：15件（前バッチ21件よりやや改善）。半七捕物帳シリーズでは主人公・脇役を
  固有名詞ではなく「岡っ引き」等の一般名詞で表現する既存運用を踏襲し、隣接話同士の登場人物取り違えは
  発生しなかった
- ARCHAIC_VOCABに新たに実例確認した語：「番頭」「芸妓」「書生」「候」（本文に実在してもいずれも
  問答無用で弾かれる。侍女・車夫等の既知リストに追加すべき語彙として記録）
- janomeの固有名詞誤検知の新パターン：「数日前」「数日後」のような数詞を伴わない時間表現を候補化
  してしまう（「数」が数詞除外リストに含まれないため）。回避策は「その後」「翌日」等、数詞を含まない
  言い回しへの統一
- 森鴎外・北原白秋・高野長英等、実在の史実人物名が本文に言及される作品が複数あったが、
  案内文では一切使用せず回避（原則8「著者名・作品名を書かない」の延長で史実人物名も慎重に扱った）

**本番第13バッチ完了（2026-08-04）：** 作品ID昇順で97件（芥川竜之介15件・岡本綺堂9件・泉鏡花9件・
夢野久作7件・有島武郎7件ほか比較的分散した構成）を生成。独立検証（`verify_production_batch13.py`）で
97/97件ゲート通過（100%）・著者名漏出0件・ラベル語彙外0件・文末多様性OK・平均字数67.3字を確認。
`state/checkpoint.jsonl`に追記（1,358行）。**累計1,358/16,203件。**
- 初回ドラフトの不合格件数：15件（過去3バッチは17件→21件→15件、今回も15件で横ばい）。原因は
  本文の表記と微妙に異なる言い回し（「雌鴉」vs本文「雌の烏」等）・ARCHAIC_VOCABの部分一致
  （「候補」の「候」等）・結末示唆表現（「最後には」）・janomeの数詞なし時間表現誤検知（既知パターン）
- **`gate_check_batch.py`ではカバーされないラベル語彙違反を、エージェント自身が追加チェックで発見**：
  「幻想」という主題ラベルを7件で使用していたが`label_vocab.py`のTHEME_VOCABに存在しない語だった
  （近い語は「怪異」のみ）。エージェントが自ら気づき、内容に応じて怪異／芸術／旅に修正。独立検証でも
  ラベル語彙外0件を確認し、この修正が正しく反映されていたことを確認した
- 実在の史実人物・著名人（森鴎外の実際の遺言書が本文だった作品、ラフカディオ・ハーンの妻の回想録、
  実在の思想家の政治的遺書、実在の染織作家を讃えた随筆等）を含む作品が複数あり、実名を一切使わず
  抽象的な記述（「ある人の遺言」「外国人教師」等）にとどめて回避
- 半七捕物帳シリーズ（9話）は引き続き主人公名を「岡っ引き」と一般名詞化する運用を継続
- 差別的表現を含む本文（跛者・黒ん坊・白痴等）は案内文に一切反映せず除外

**本番第14バッチ完了（2026-08-04、同日2セット目）：** 作品ID昇順で97件（海野十三37件・大阪圭吉14件・
岡本かの子9件・岡本綺堂8件・泉鏡花6件・伊丹万作6件ほか混在）を生成。独立検証
（`verify_production_batch14.py`）で97/97件ゲート通過（100%）・著者名漏出0件・ラベル語彙外0件・
文末多様性OK・平均字数66.8字を確認。`state/checkpoint.jsonl`に追記（1,455行）。**累計1,455/16,203件。**
- 初回ドラフトの不合格件数：13件（過去4バッチは17→21→15→15、今回13でさらに改善）
- 海野十三・大阪圭吉の探偵小説群では、既存の捕物帳シリーズと同様に探偵役の固有名詞（帆村荘六等）を
  「探偵」「私立探偵」等の一般名詞に置き換える運用で登場人物取り違えを回避
- 岡本綺堂による中国怪奇小説の翻訳作品（1298〜1300番）で、既存の時代区分ラベル（西欧・ロシア・北欧等）
  に該当するものがなく、「異世界・時代不詳」ラベルを使用（`label_vocab.py`に実在する語彙で、
  独立検証でも語彙外0件を確認済み）
- 「鶴は病みき」（1283番）は実在の著名作家（芥川龍之介がモデル）を扱った準実名の回想記だったが、
  本文中の仮名をそのまま使わず「高名な作家」と抽象化して回避
- 「給仕」がARCHAIC_VOCABに新たに抵触することを確認（侍女・車夫等の既知リストに追加すべき語彙）
- **今回、エージェントが作業用の一時ファイル（scratch_*, compose_*.py等）を自ら削除して報告してきた。**
  前回（第13バッチ）で呼び出し側が後始末する事態になったため、プロンプトに明記して対応した結果、
  残置なしで完了した

**本番第15バッチ完了（2026-08-05）：** 作品ID昇順で97件（菊池寛24件・岡本綺堂10件・幸田露伴9件・
黒島伝治7件・甲賀三郎6件ほか混在）を生成。独立検証（`verify_production_batch15.py`）で97/97件
ゲート通過（100%）・著者名漏出0件・ラベル語彙外0件・文末多様性OK・平均字数61.1字を確認。
`state/checkpoint.jsonl`に追記（1,552行）。**累計1,552/16,203件。**
- **初回ドラフトの不合格件数：96/97件と急増（過去5バッチは17→21→15→15→13）。** 原因は手戻り件数の
  傾向悪化ではなく、初回ドラフトが軒並み60字の下限をわずかに割り込んでいたという単発の書き方の
  クセによるもの（固有名詞や現代語ゲート由来の「本物の欠陥」はごく一部）。数字だけを機械的に
  トレンドとして追うと誤解を招く実例。この指標は「原因を伴って読む」ものであることを再確認
- 甲賀三郎の探偵小説では、既存の捕物帳・探偵小説シリーズと同様に探偵役を一般名詞化する運用を継続
- 尾崎秀実「遺書」（死刑囚の実際の遺書）、梶井久「臨終まで」（実子の死の記録）など重い実話性の
  高い作品は、名前を出さず抽象的な言葉遣いで対応
- 菊池寛の合戦史論考シリーズ（16篇）では、信長・秀吉・家康等の史実人物名を、物語に不可欠な要素として
  そのまま使用（8章「羅生門」の地名と同じ判断枠組み。案内文の対象作品自身の著者名ではなく、
  作品が論じる対象としての史実人物名である点に注意）

**本番第16バッチ完了（2026-08-06）：** 作品ID昇順で97件（太宰治24件・佐々木味津三12件（旗本退屈男・
右門捕物帖）・島崎藤村11件・高村光雲11件（幕末維新懐古談）・小酒井不木6件ほか混在）を生成。
独立検証（`verify_production_batch16.py`）で97/97件ゲート通過（100%）・著者名漏出0件・
ラベル語彙外0件・文末多様性OK・平均字数67.1字を確認。`state/checkpoint.jsonl`に追記（1,649行）。
**累計1,649/16,203件。**
- 初回ドラフトの不合格件数：12件（前バッチの96件は字数下限の書き方のクセが原因と判明していた通り、
  今回は事前に注意喚起した結果、通常水準（過去は17→21→15→15→13）に戻った）
- 佐々木味津三「旗本退屈男」シリーズは主人公名（早乙女主水之介）を「退屈をもてあました旗本」に
  一般化。同著者の別シリーズでは実在の幕末史実人物（老中・藩主等）が登場するため役職名に一般化し、
  人名としては出さなかった（史実人物でも、作品が論じる対象そのものでない限り一般化する運用）
- 島崎藤村の自伝的作品群では、家族の固有名（半蔵・お種等）を使わず「宿場を継いだ当主」
  「旧家の主婦」等に一般化。太宰治作品の地の文に著者本人のペンネームが登場する例も複数あったが、
  案内文には一切使用せず
- janomeの固有名詞誤検知（「幾筋」「母娘」「何度」等、非固有名詞の複合語）が今回も複数件発生。
  個別の語彙追加はせず、都度言い換えで対応する既存方針を継続

**本番第17バッチ完了（2026-08-07）：** 作品ID昇順で97件（田中貢太郎49件（中国怪談・伝奇の翻案が
中心）・寺田寅彦9件・太宰治8件・直木三十五6件ほか混在）を生成。独立検証（`verify_production_batch17.py`）
で97/97件ゲート通過（100%）・著者名漏出0件・ラベル語彙外0件・文末多様性OK・平均字数66.6字を確認。
`state/checkpoint.jsonl`に追記（1,746行）。**累計1,746/16,203件。**
- 初回ドラフトの不合格件数：6件（過去水準の中でも最良）。田中貢太郎49件という極端な著者集中に対し、
  生成エージェントは自らの判断でID範囲ごとに5つのサブエージェントへ分割し並列処理した
  （呼び出し側の指示は「1回でまとめて処理」だったが、規模を見て自律的に変更した）
- **この並列分割が、権限プロンプトの急増という副作用を生んだ。** `production_batch17_part1〜5.json`
  という中間ファイルが作られ、`gate_check_batch.py --all`も5回個別に呼ばれたため、
  同種のツール呼び出し承認が単純計算で5倍に増えた。運営者からの指摘を受け、この回を機に
  `.claude/settings.local.json`のBash許可をワイルドカード方式（`Bash(python *)`等）に切り替え、
  今後は個別コマンドの完全一致待ちを減らす対応をとった
- 中間ファイル（part1〜5）とスクラッチファイルはエージェントが自ら削除し、残置なしで完了

**本番第18バッチ完了（2026-08-08）：** 作品ID昇順で97件（萩原朔太郎15件・原民喜11件・林不忘9件・
内藤湖南5件・浜尾四郎5件（探偵小説）・林芙美子5件・牧逸馬5件・牧野信一5件ほか混在）を生成。
独立検証（`verify_production_batch18.py`）で97/97件ゲート通過（100%）・著者名漏出0件・
ラベル語彙外0件・文末多様性OK・平均字数68.1字を確認。`state/checkpoint.jsonl`に追記（1,843行）。
**累計1,843/16,203件。**
- 初回ドラフトの不合格件数：10件
- **前回バッチの反省を踏まえ、「分割は3グループまで・最終検証は1回にまとめる」と明示的に指示した
  結果、想定通りに機能した。** エージェントは3グループ（33/32/32件）に分けて下書きしたが、
  最終的な`gate_check_batch.py --all`は97件をマージした1ファイルに対して1回だけ実行し、
  ツール呼び出し・権限プロンプトの急増は再発しなかった
- **牧逸馬・林不忘は同一人物（長谷川海太郎）の別筆名という罠を事前に伝えたところ、
  互いの筆名・本名が案内文中に一切現れないことを確認済み**（エージェント自身が全97件を
  対象に両筆名・本名で機械的に検索し0件を確認、独立検証でも同様に0件）
- 原民喜の原爆関連作品は、地名を断定せず、被害描写も具体化しない抽象的な表現で対応
- 犬の生活（1783番）で「武蔵野市」という1947年以降の地名が本文にあったため、
  時代区分ラベルを「昭和初期」ではなく「昭和戦後」に修正した判断が適切だった

---

## 【重要】案内文の「あらすじ化・ネタバレ」問題の発見と対処（2026-08-09〜12）

運営者から「案内文が結末まで書いてしまっているのでは」という指摘を受け調査した結果、
**入力を本文冒頭「固定4,000字」としていた旧ルールが、本文が短い作品ほど露出率
（min(4000,文字数)÷文字数）を押し上げ、結末や終盤の伏線までAIに見せてしまっていた**
という構造的欠陥が判明した。詳細は以下の一次資料を参照。

- `docs/SPOILER_ISSUE_BRIEF.md` — 発見の経緯と初期調査（後に規模の見立てを訂正）
- `docs/GUIDE_TEXT_SPEC_V2.md` — 確定仕様（新ルール・B型・入力境界の規律）
- `docs/STAGE2_CONSULT.md` / `docs/STAGE2_SPEC.md` — 第2段階着手前の相談・仕様
- `docs/PIPELINE_V2_SPEC.md` — 本番パイプライン適用の仕様・パイロット設計
- `docs/pilot_report.md` — パイロット24件の結果

### 対処の全体像

1. **新ルールの確定**：入力を「本文の25%（下限300字・上限4,000字）」に変更。
   物語系1,145件のうち露出率40%超（464件）を遡及修正の対象とした
   （40%未満・非物語系は「実地確認で良好域」として対象外。この線引きは未再検証のまま）
2. **B型の新設**：文字数1,600字未満の作品向けに、筋を追わず「様式予告」「状況アイロニー」
   という2つの引きの型を確立（実地5件の試作で検証）
3. **入力境界の規律**：結末を明かしていなくても、新入力より先の情報を使う「境界侵犯」
   （桜の樹の下には・指の例）も、ゲートでは検出できない問題として明文化
4. **遡及修正（第1〜3段階、合計464件）**：露出率90%以上（169件）→60〜90%（143件）→
   40〜60%（152件）の順に、エージェントによる修正案作成→独立検証→運営者確認→マージ、
   という手順で完了。全て`checkpoint.jsonl`に`由来=revision_stageN`として追記
5. **本番パイプラインへの適用（バッチ19〜）**：`extract_batchN_fulltext.py`の切り出し
   ロジックを新式に変更。適用前に非物語系16件・物語系8件のパイロット24件で検証し、
   B型随筆1件で主題の食い違いを発見・対処（次項）

### 遡及修正の実績（第1〜3段階）

| 段階 | 露出率 | 件数 | 修正 | 変更不要 | ゲート | 著者名漏出 | 91-100字の割合 |
|---|---|---|---|---|---|---|---|
| 第1段階 | 90%以上 | 169件 | 159件 | 10件 | 100% | 0件 | 81%（反省点） |
| 第2段階 | 60〜90% | 143件 | 122件 | 21件 | 100% | 0件 | 42.7% |
| 第3段階 | 40〜60% | 152件 | 110件 | 42件 | 100% | 0件 | 2.6% |

字数の偏り（第1段階の反省）は、指示の明確化により段階を追うごとに改善した。

**副産物として2つの構造的欠陥を発見・修正：**
- 著者名漏出チェックが`著者ID`→`authors.json`という経路（共著・ID空で欠落）に依存しており、
  第1段階169件中20件でチェックが素通りしていた（事後照合で実害0件は確認済み）。
  `index.json`の`著者`欄を直接使う方式に切り替えて再発防止（第2段階以降）
- 通常バッチの検証スクリプト（`verify_production_batchN.py`）も、著者名を`.split()[0]`
  （先頭トークンのみ）で照合しており、共著の2人目・雅号を見逃す構造だった。
  `verify_production_batch19.py`から、上記と同じ頑健なロジックに置き換えた
  （**過去バッチ1〜18への遡及適用はまだ実施していない**）

### パイロット検証（docs/PIPELINE_V2_SPEC.md 5章）で判明した重要事項

遡及修正は物語系のみを対象としてきたため、**非物語系（随筆・詩・評論等、全体の34.4%）は
新ルール下で一度も検証されていなかった。** パイロット24件（非物語系16・物語系8、著者重複なし）
で検証した結果：

- 23/24件は主題一致、6件は「新版が旧版の入力境界侵犯を正しく修正した」成功例
- **1件（容貌・太宰治、B型725字）で明確な主題の食い違いを確認。** 300字の新入力が
  随筆前半の話題だけで終わり、旧案内文が描いていた後半のエピソードに届いていなかった
- 判定基準（24件中3件以上で危険）には該当せず、本格適用してよいと判断
- 運営者承認により、B型随筆の話題転換への注意（`docs/GUIDE_TEXT_SPEC_V2.md` 3-4b節）を追加

### バッチ19（新ルール本番初適用）

- 切り出しロジックを`full_text[:4000]`から`min(4000,max(300,int(n*0.25)))`に変更。
  97件全てで検算一致を確認。**平均露出率は旧47.2%→新21.7%まで改善**
- 宮本百合子56件（58%）・宮沢賢治19件（20%）で合計77%という過去最高の著者集中があったが、
  連作（1993/1994、2003/2004/2011/2012/2015、2013/2014等）の固有名詞混同なく処理できた
- 字数分布は91〜100字が0件（過去最良）、平均71.4字
- 詳細は下記の通常バッチ記録を参照

---

**本番第19バッチ完了（2026-08-12）：** 作品ID昇順で97件（宮本百合子56件・宮沢賢治19件・
森鴎外3件ほか混在。**新ルール本番初適用**）を生成。独立検証（`verify_production_batch19.py`、
今回から著者名漏出チェックを頑健化）で97/97件ゲート通過（100%）・著者名漏出0件・
ラベル語彙外0件・文末多様性OK・平均字数71.4字を確認。`state/checkpoint.jsonl`に追記（2,404行）。
**累計1,940/16,203件。**
- 初回ドラフトの不合格件数：12件。原因は本文にない表現（「夕風」→「風」等）と
  ARCHAIC_VOCABの部分一致誤検知（「侍従候補」の「候」）
- 字数分布：60-70字42件・71-80字51件・81-90字4件・**91-100字0件**（過去最良の締まり）
- 形式内訳：物語系83件（小説64・童話18・戯曲1）、非物語系14件（随筆6・評論3・書簡3・論考1・詩1）
  ——非物語系の割合（14.4%）は予測（34.4%）より低かった。著者構成の偏りによるもので、
  次バッチ以降も同程度とは限らない
- 道標（2012、848,542字）・雪之丞変化（1911、358,666字）・伸子（2015、241,229字）という
  超長編3件を含んだが、新ルールでも上限4,000字は変わらず、挙動に問題なし
- 「モスクワ」→本文表記「モスク」、「ドイツ」→本文表記「独逸」等、現代表記への
  引きずられが複数件あったが自己検証段階で修正済み

---

**本番第20バッチ完了（2026-08-21）：** 作品ID昇順で97件（夢野久作48件・横光利一11件・
蘭郁二郎8件・森鴎外7件・吉行エイスケ5件ほか混在。**単著集中48/97=49.5%は過去最高水準**）を
生成。独立検証（`verify_production_batch20.py`）で97/97件ゲート通過（100%）・著者名漏出0件・
ラベル語彙外0件・文末多様性OK・平均字数70.2字を確認。`state/checkpoint.jsonl`に追記（2,501行）。
**累計2,037/16,203件。**
- **著者自身の雅号による著者名漏出の罠を新たに発見・対処：** 作品ID2096（黒白ストーリー）・
  2142（梅津只円翁伝）はindex.json上の著者欄が「夢野久作、杉山萠円」という共著表記だが、
  **杉山萠円は夢野久作自身が随筆・伝記等で使った別の雅号（同一人物）**。事前にこの罠を
  エージェントへ伝え、両表記・「萠円」単体・本名「泰道」も含め一切使用しないことを
  確認（独立検証の著者名漏出チェック＝著者欄の全氏名候補照合でも0件を確認）
- 同一タイトルの別作品（作品ID2077森鴎外作「夢」・2170吉江喬松/孤雁作「夢」）が
  偶然同バッチに含まれたが、それぞれ入力データのみを見て独立に執筆され混同なし
  （呼び出し側の事前指示で2077の著者を誤って「夢野久作」としていたが、エージェントが
  実際の本文データから正しく森鴎外作と判断し訂正した）
- 夢野久作の探偵小説・怪奇小説群では、既存の捕物帳・探偵小説シリーズと同様に
  主人公・探偵役を一般名詞化（「探偵」「刑事」「機関長」等）する運用を継続、
  同一著者内の隣接作品間での固有名詞取り違えは発生しなかった
- 超長編3件（伊沢蘭軒491,896字・暗黒公使219,339字・巴里より176,343字）を含んだが、
  新ルールでも上限4,000字は変わらず挙動に問題なし
- janomeの固有名詞誤検知の新パターンを2件確認：「仇討ちの」が「討」「ちの」に
  誤分割され「ちの」を地名固有名詞と誤判定／「岩の上」がjanome辞書に地名として
  単独登録され誤検知。個別語彙追加ではなく言い換えで解消する既存方針を継続
- 呼び出し側の作業ミス：`merge_batch.py`を誤って2回実行し、checkpoint.jsonlに
  同一内容が97行×2で重複記録された（D配列自体は`by_id`辞書経由の参照更新のため
  実害なし、idempotent）。マージ前に内容完全一致を確認した上で重複分を削除して対処

---

**「新着文庫」ページ 実装・公開完了（2026-08-01）：** 青空文庫本家の新規公開作品一覧
（`https://www.aozora.gr.jp/index_pages/whatsnew1.html`、直近半年分50件）を`whatsnew.json`として
取得し、`whatsnew.html`（新規ページ、日付ごとにグルーピング、各作品に入力者・校正者名を明示し
図書カードへ直リンク）を作成。ホームのヘッダーに「新着文庫を見る」リンクを設置。
`kumoma-pipeline`・公開サイト`kumoma`の両方にpush済み、GitHub Pagesに反映済み。
**雲間側の案内文生成とは独立した機能**（新着作品は測定・案内文生成の対象にしない）。
このページ用データ(`whatsnew.html`内の`NEWS`配列)は今後、本家の新着一覧を随時再取得して更新する必要がある
（自動化はまだ未実装。運営者の判断で手動更新するか、更新頻度を検討する）。

**サイトの数値表示について（2026-08-05追加）：**
- トップページのフッター統計に「案内文つきは○○篇」を追加した。`D`配列から`w[16]`が非nullの件数を
  都度計算する形なので、ハードコードではなく常に正確。今後のバッチでも追加対応不要
- `whatsnew.html`に、青空文庫全体の収録数（`AOZORA_STATS`定数：総数・著作権なし・著作権あり・確認日時）
  を表示する行を追加した。出典は`aozora.gr.jp`トップページのフッター表示（CORS制限により
  ブラウザから直接ライブ取得はできないため、静的な値を手動更新する方式）。運営者の要望で
  「いつ取得した数字か」を時刻まで表示するようにした（`確認日時`はJSTで記録し、表示時は
  `+09:00`を明示してタイムゾーンに依存せず日本時間で出す）。次回更新時はこの値を上書きすること
- **運営者から「新着文庫の更新は週一で見直したい」との要望あり。** クレジットを使わずに実現する方法は
  ない（`RemoteTrigger`はクラウドルーティンでクレジット消費、`CronCreate`はセッション生存中のみ・
  最大7日で失効し確実な長期リマインドには使えない）。そこで**バッチ作業等でセッションを開くたびに、
  whatsnew.htmlの`AOZORA_STATS.確認日`から経過日数を機械的にチェックし、7日以上経っていたら
  「新着文庫とAOZORA_STATSの見直し時期」であることを一言添える運用にする**（追加コストなし。
  ただしセッションを開かない限り気づけない制約はある）。次回セッション開始時、この判定を実施すること

**Instagramアカウントの運用を開始（2026-08-01〜）：** 案内文パイプラインとは別プロジェクトとして
`C:\Users\81908\Desktop\kumoma-instagram`（GitHubリポジトリ`inouemst/kumoma-instagram`、private）を
新設。私（Claude）の役割はコンテンツ制作（投稿画像テンプレート・キャプション案）のみで、
アカウント開設・実際の投稿操作は運営者が行う。投稿の軸は「案内文の一節を主役に、入力者・校正者名を
必ず明示する」——ボランティア文化の可視化という雲間の使命に直結させる設計。詳細・原則はそちらの
CLAUDE.mdを参照。**この案内文生成パイプライン側のCLAUDE.mdにはInstagram関連の作業ログを書き足さない**
（プロジェクトを分けた意図が薄れるため）。

### 第2回ベンチマーク（2026-07-25、プロンプト修正後・独立検証済み）

較正週で見つかった欠陥（著者名記載・固有名詞の現代化）への対策として、プロンプトに
「著者名・作品名を書かない」「本文の表記を絶対に変えない」「生成後にARCHAIC_VOCABと
照合し自己修正する」を追加し、97篇フルベンチマークをN=3/8/15で再実施した。
**エージェントの自己申告（全件通過）は鵜呑みにせず、`verify_benchmark2.py`でgates.pyを
直接実行して独立に再検証した。結果は以下の通り、自己申告と食い違う部分があった。**

| 項目 | 合格線 | N=3 | N=8 | N=15 |
|---|---|---|---|---|
| 第三層(固有名詞)通過率 | 95%以上 | **91.8%**(自己申告100%は誤り) | 100% | 100% |
| 固有名詞Jaccard(対 手書き版) | 0.6以上 | 0.281 | 0.291 | 0.334 |
| 字数規格 | 100% | 100% | 100% | 100% |
| 型一致率 | 90%以上 | 97.6% | 97.6% | 98.8% |
| 文末多様性 | 各20%未満 | OK | OK | OK |
| 総合ゲート通過率 | — | 91.8% | 100% | 100% |

**結論：**
- 著者名記載・固有名詞の現代化という「本物の欠陥」への対策は効いた。**N=8とN=15は固有名詞の実在チェックを100%達成**（較正週は全N未達だった）
- **N=3だけ今回は基準未達（91.8%）。** 前回の較正週はN=8が最下位だったが、今回はN=3が最下位——N依存の単調な関係はなく、サンプル間のばらつきの可能性がある
- **Jaccard(手書き版との一致度)は3パターンとも0.6を大きく下回ったまま(0.28〜0.33)。** 固有名詞の正確さ(実在確認)は大きく改善したのに一致度は改善しなかった。これは「別々の書き手が同じ素材から違う固有名詞を選ぶ」自然な差である可能性が高く、**この基準(0.6)自体が創作的な圧縮作業に適切かどうか、運営者の再検討が必要**
- 生成結果は`benchmark2_n3/8/15.json`、検証スクリプトは`verify_benchmark2.py`

**推奨：** 固有名詞の正確さ・型一致・文末多様性が良好なN=8またはN=15を本番候補とし、
Jaccard基準は「参考値として記録するが合否には使わない」方向で運営者に確認する。

**→ 運営者確認済み（2026-07-25）：本番のバッチサイズは N=15 に確定。** Jaccard基準は
合否判定には使わず、参考値として記録する運用とする。

### 較正週は実施済み（2026-07-25、詳細は `docs/CALIBRATION_REPORT.md`）

クラウドルーティン(1回)で較正週を実施した。結論：**本番移行はまだ早い。**

- **Pass A（2,000字ラベル先行）：不採用確定。** 4軸すべて合格線未達
- **N∈{3,8,15}ベンチマーク：全滅。** ただし原因はバッチサイズではなく**プロンプト側の具体的な欠陥2種**と特定できた：
  1. 著者名・作品名を案内文に書いてしまう（本文に出てこないので第三層に弾かれる。36件中6件）
  2. 固有名詞の現代化・誤用が一部再発（`389`たけくらべの偽「吉原」など、CLAUDE.md既知の罠そのものが再発。36件中4件、全291生成中約1.4%）
  - 残り26件はゲート側の誤検知（言い換えを固有名詞と誤認識）で実害なし
- **`fulltext_100.json` は再生成済み。** 生成入力用（4,000字）と、`fulltext_100_full.json`（ゲート正解データ用・全文）に**分離した**。理由：ゲート照合の正解データまで4,000字に絞ると、境界直後に初出する正当な固有名詞まで誤って弾いてしまうため（`gates.py`自身の自己テストで発覚）。**gates.pyのロジックは変更していない。渡すデータを使い分けただけ**
- 軽量版リポジトリは不採用（本文がプレーンテキストでXHTML非互換）。本家を sparse-checkout で使用、100作品で約1.1GB
- スループット参考値：N=8/15で約130〜140件/時間。全16,203件で粗い試算1〜3ヶ月（引き継ぎ書の推定と大きく矛盾しない）

**次にやること：** 上記のプロンプト欠陥2点を直し、小規模（15〜20篇程度）で再テストしてから、97篇フルベンチマークに戻る。97篇×3パターンをいきなりやり直さない（コストが大きいため）。

### 実行環境について（2026-07-25 追記）

**クラウドルーティン（CCR）は、Proプランの利用枠とは別の「クレジット」残高を消費する。** ローカルのこのセッション（Claude Code CLI）は引き続きPro/Maxの共有利用枠を使う。**運営者の希望：基本はローカルの利用枠でやりくりし、クレジット消費はなるべく避ける。** 小規模な検証・作業はローカルセッションで行い、クラウドルーティンは「PCを離れる時にどうしても必要な場合」に限定する。

---

## 1. このプロジェクトの原則（すべての判断の土台）

- **上書きしない** — 本文には触れない。案内文は本文の代わりではなく、入口
- **序列をつけない** — 「傑作」「名作」「代表作」等の評価語を使わない
- **寄生しない** — Wikipedia・文学事典・既存の解説を参照しない。本文だけを見る
- **書けないものは、書けないと言う** — もっともらしい嘘より空欄の方がよい
- **測れないものは、測れないと言う** — 検証していないことを「検証済み」と書かない

最後の2つが最重要。都合のよい条件下でだけ動く検証を、動作の証明として報告しない。

---

## 2. 【最重要】お金の話 — 毎セッション最初に確認

Pro以上の購読に含まれ追加課金なしで使えるが、罠が一つある：

> `ANTHROPIC_API_KEY` 環境変数が設定されていると、購読ではなくAPI従量課金になる。

**セッション開始時に毎回 `/status` を実行し、サブスクリプション認証を目視確認すること。**
API キー経由になっていたら作業を止めて運営者に報告する。

利用枠はclaude.aiチャットと共有。枠を使い切ったらその日は終わり（自然な日次区切り）。

---

## 3. 完成済み成果物の扱い

| ファイル | 中身 | 扱い |
|---|---|---|
| `index.json` | 全17,456作品の計測値・はじまりの一文 | **読むだけ。絶対に変更しない** |
| `authors.json` | 著者1,018人の集約 | 読むだけ |
| `under200_ids.json` | 本文200字未満895件のID | 除外リストとして使う |
| `body_parser.py` / `build_index.py` / `finalize.py` | 第一段階のパイプラインコード | 復旧済み。再利用する |
| `gates.py` / `label_vocab.py` | 品質ゲート・ラベル語彙（第2便版） | 復旧済み・動作確認済み。再利用する |
| `drafts_batch1.json` | 手書き97篇の案内文（ベンチマーク対照群） | 復旧済み（2版あった。採用理由は上記参照） |
| `selection100.json` / `labels_pass1,2.json` | 100篇の選定・ラベル2回分 | 復旧済み |
| `fulltext_100.json` | 100篇の本文冒頭4,000字（**生成入力用**） | 較正週で再生成済み |
| `fulltext_100_full.json` | 100篇の本文全文（**ゲート正解データ用**） | 較正週で新規作成。第三層の照合はこちらを使う |
| `benchmark_n3/8/15.json` / `label_2000char_test.json` | 較正週の生成結果一式 | 参照用。次の再テストの比較対象にできる |
| `docs/CALIBRATION_REPORT.md` | 較正週の詳細報告書 | 一次資料。要約は本ファイル冒頭 |

`index.json`の「計測値」「はじまりの一文」は第一段階の確定成果。第二段階では一切変更しない。

---

## 4. これまでに判明した罠（同じ穴に落ちないための地図）

### データ取得
- `--filter=blob:none` のまま全件処理しない。ネットワーク取得が毎回発生する。sparse-checkout（coneモード）＋ `--depth 1`
- 本文ファイル名は `{作品ID}_{ファイルID}.html` だが `{作品ID}.html` という別パターンも実在（見落とすと誤って取得不能扱いになる）
- 文字コードは Shift_JIS

### 本文パース
- 本文の終端は `<div class="bibliographical_information">`。**`bibliography_information`ではない**
- カギ括弧の深さは行ごとにリセットする（文書全体で追跡すると複数段落の会話で破綻する）

### 判定ロジック
- 旧仮名＝文語ではない（独立した軸）
- 会話文比率は戯曲では機能しない（「」を使わないため）
- 閾値を1作品だけで「検証済み」としない

### 案内文
- 固有名詞は本文の表記のまま（豐太郎／淺野／亜米利加、等。現代表記に直さない）
- 本文にないことを具体化しない（「わが國」を「日本」と書かない）
- 本文が短すぎると案内文は書けない → 200字未満は対象外
- **著者名・作品名を案内文に書かない（2026-07-25 較正週で決定）。** 生成時に著者名・作品名をプロンプトに渡しているが、本文自体には著者名が出てこないため、書くと第三層ゲートに必ず弾かれる。随筆・評論で「〜が回想する随筆」のように書きたくなるが、本文中の言い回しだけで表現する
- **既知の罠（吉原、豐太郎等）を個別に潰しても、同種の罠が別の固有名詞で再発しうる。** 較正週で「たけくらべ→偽の吉原」が対策後も再発し、「安兵衞→安兵衛」「ニイチェ→ニーチェ」という新たな現代化ミスも見つかった。個別事例の暗記ではなく「本文にない表記は絶対に使わない」という原則そのものを徹底する必要がある

### ゲート
- 自己申告に依存するゲートはゲートではない。機械抽出（janome）に一本化
- 誤検知を消すために語彙リストへ語を追加しない。除外は「形の性質」で行う
- 文末集計は表層4文字で数えない（活用形の違いを別物として数えると偏りが過小評価される）

---

## 5. ゲートの仕様（`gates.py` 再構築の指針）

| 層 | 内容 | 実装方針 |
|---|---|---|
| 第三層 | 固有名詞が本文に実在するか | janome形態素解析で固有名詞抽出＋数詞除外。自己申告は使わない |
| 第四層 | 非流通語（古語）混入なし | ブラックリスト方式 |
| 第五層 | 60〜100字／評価語なし／結末示唆なし | 機械チェック |

禁止表現（docs/GUIDE_TEXT_SPEC.md 4-3）：
不朽の名作／珠玉の一篇／心温まる／考えさせられる／胸を打つ／色褪せない／今なお／傑作／必読／
〜な人におすすめ／読者に問いかける／深く突き刺さる／人間の本質／普遍的なテーマ

---

## 6. 確定している設計判断

### 生成対象は16,203件
```
全作品 17,456件 − 200字未満 895件 − 表記違い統合 358件 = 16,203件
```
統合条件：同一著者・同一題名・文字数±10%以内・文字遣いが互いに異なる352組。
文字遣いまで同じ23組は統合しない（別作品の可能性）。`index.json`のエントリ自体は統合しない。

### 入力は本文の先頭4,000字のみ
結末を見せない構造的対策（「書くな」ではなく「見せない」）。

### 案内文の型は2つ
| 対象 | 型 |
|---|---|
| 物語 | 誰が・どこで・何をする話か |
| 随筆・評論 | 何を手がかりに・何について、どう論じているか |

### 「育つ庭」モデル
全16,203件の完成は公開条件ではない。生成順序はアクセスランキング順（読まれる作品から）。
UI側の規律（申し送り事項）：
1. 案内文の有無で並べ替えない
2. 「準備中」ラベルを出さない
3. 案内文がない作品も同じ視覚的重みで表示する

---

## 7. 較正週（実施済み。結果は `docs/CALIBRATION_REPORT.md`）

2026-07-25にクラウドルーティンで実施済み。結果の要約は0章参照。**再ベンチマークが次の課題。**

**次にやること（本番移行の前提条件）：**
1. プロンプトに以下を追加：
   - 「著者名・作品名を案内文に書かない」を明記
   - 「本文にない表記・現代化した表記は絶対に使わない」を、個別事例の暗記ではなく原則として徹底する言い回しに強化
2. **97篇まるごとではなく15〜20篇程度の小規模で**、上記の修正が効くか再テストする（ローカルセッションで、コストを抑えて）
3. 改善が確認できたら、97篇フルベンチマーク（N∈{3,8,15}、合格線は前回と同じ）に戻り、本番のNを確定する
4. 実測スループット確定 → 完了予定日を算出 → 運営者が第3便仕様書を改訂

**合格線（変更なし）**：固有名詞通過率95%以上／手書きとのJaccard0.6以上／字数規格100%／型一致90%以上／文末偏り各20%未満。
**約束ではなく測定で進む。全項目を満たす最大のNを採用。満たすNがなければプロンプト側の問題として報告する。**

---

## 8. 本番運用（較正が終わってから）

毎日の型：
1. `/status` で認証確認
2. `state/checkpoint.jsonl` を読み、優先順リストの未処理IDを取得
3. N件ずつ処理：本文先頭4,000字を読む→案内文＋4軸ラベルをJSON化→`gates.py`にかける
   →不合格なら修正（上限3回）→3回落ちたら`案内文なし理由`を確定して次へ
4. 1件ごとにcheckpointへ追記（クラッシュ安全）
5. 枠が尽きたら進捗サマリ（済/残/推定完了日）を出して終了

出力形式：
```json
{
  "作品ID": "127",
  "案内文": "夜、羅生門の下で雨宿りする男。...",
  "案内文なし理由": null,
  "形式": ["小説"],
  "主題": ["貧困", "死", "身分・階級"],
  "読後感": ["不穏", "張りつめた"],
  "時代の質感": ["平安"]
}
```

`案内文なし理由`を必ず使えるようにしておく。書かせない逃げ道がないと必ず何か書いてしまう。

## 人手検品（第六層）— 運営者の仕事
層化抽出で200〜400件。判定ははい／いいえ／判断保留の三択のみ。
厚く見る層：随筆・評論／文語・旧字旧仮名／長大な作品／ゲートを2回以上落ちて通った作品（全件）。

### 8-1. 新しいバッチを公開サイトに反映する手順（2026-07-26確立）

生成が終わり検品も済んだら、次の手順で公開する。

1. `kumoma-pipeline`（このリポジトリ）側で、`kumoma_prototype.html`の`const D=[...]`に
   新規分の案内文・形式・読後感をマージする（作品IDで突き合わせ、`w[16]`=案内文、
   `w[17]`=形式、`w[18]`=読後感。無ければ`null`のままにする＝「育つ庭」の実装そのもの）
2. `state/checkpoint.jsonl`に1件ごと追記する
3. 公開用リポジトリ`kumoma`を別フォルダにクローンし（例：`C:\Users\81908\Desktop\kumoma-site`）、
   更新した`kumoma_prototype.html`を`index.html`としてコピーして commit・push する
4. GitHub Pagesが自動でビルドする（`gh api repos/inouemst/kumoma/pages/builds/latest`で状況確認可）。
   数十秒〜1分で反映される
5. `kumoma-pipeline`側にも変更後の`kumoma_prototype.html`をcommit・pushしておく（一次情報源はこちら）

**`kumoma`（公開）と`kumoma-pipeline`（内部作業）は別リポジトリ。** 混同しないこと。

---

## 9. まだ決まっていないこと（勝手に決めない）

1. 実スループット — 参考値は出たが確定値ではない（較正週：N=8/15で約130〜140件/時間。要実測、ゲート再試行込みの実運用速度は未計測）
2. 読後感ラベルを残すか落とすか — 保留中
3. 「淡々」と「端正」の統合 — 検討中
4. カタカナ抽出器の構造的な誤検知（較正週3-2節C）— 語彙追加ではない解決策が見つかっていない
5. 本番の全16,203件クローン時のディスク使用量 — cone modeのsparse-checkoutは著者ディレクトリ単位でしか絞れず、100作品で1.1GBだった。全件だと数十GB規模になりうる。non-cone（パターン指定）での削減余地は未検証
6. コミット著者情報の不一致（stop hookが指摘）— git config変更は指示上禁止のため未対応。対応要否は運営者判断

**決着した項目（もう議論しない）**：Pass A採否（不採用確定）／軽量版リポジトリ採否（不採用確定）／運営者のプラン（Pro確認済み）／著者名・作品名の言及可否（不可、4章に明記済み）／**本番のバッチサイズ（N=15に確定、2026-07-25）**／Jaccard基準の扱い（合否判定に使わず参考値扱い）

---

## 10. 最後に

検証していないことを検証済みと言わない。破れなかったという報告より、破れ方を見つけた報告の方が価値がある。急がない。
