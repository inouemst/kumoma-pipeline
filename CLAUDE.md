# 雲間 — 案内文・ラベル生成プロジェクト

> このファイルはClaude Codeが毎セッション自動で読む指示書。
> ベース：`KUMOMA_HANDOVER_TO_CLAUDE_CODE.md`（2026-07-24付、claude.ai上のOpus作成）。
> 詳しい経緯・罠の記録・原則の理由はそちらの原文を参照すること。このファイルはその要約＋現在地。

---

## 0. 三十秒で分かる現在地（2026-07-24 更新）

| 段階 | 内容 | 状態 |
|---|---|---|
| 第一段階 | 全作品の機械計測 | **完了**（17,456件、`index.json`） |
| 第二段階 第1便 | 案内文パイプライン構築、100篇実証 | 完了（済んでいるはずだが下記「現状のギャップ」参照） |
| 第二段階 第2便 | ラベル体系構築、100篇実証 | 同上 |
| **第二段階 第3便** | **16,203件の全件生成** | **← 今ここ。較正週すら未着手** |

**いきなり生成を始めない。** 較正週（第7章）から。

### 現状のギャップ（2026-07-24 復旧作業の記録）

過去のclaude.ai会話からのzip書き出しを運営者が発掘し、大半のファイルを復旧できた。

- `under200_ids.json` — `index.json` から再計算し復元済み（895件、引き継ぎ書の数値と一致）
- `body_parser.py` / `build_index.py` / `finalize.py` — 第一段階のzipから復元
- `gates.py` / `label_vocab.py` / `labels_pass1.json` / `labels_pass2.json` — 第2便のzipから復元
  （gates.pyはjanome機械抽出版。動作確認済み・`pip install janome`実行済み）
- `drafts_batch1.json` / `selection100.json` — 第1便のzipから復元

**要注意：`drafts_batch1.json`は第1便フォルダと第2便フォルダの両方に、内容が異なる2バージョンが存在した。**
比較の結果、第1便フォルダ版を採用した。理由：第2便フォルダ版には本ファイル4-4節で記録した
まさにその誤り（「わが国」を「日本」と書く／「亜米利加」を「アメリカ」と書く）がそのまま残っており、
第1便フォルダ版はそれが修正された文面になっていた。**この判断は状況証拠によるもので、
運営者による最終確認が済んでいない。** 較正作業で不審な点があれば、まずこの点を疑うこと。

**`fulltext_100.json` はまだ見つかっていない。** 5つのzipのどれにも含まれていなかった。
これは人手データではなく本文からの機械抽出物なので、aozorabunkoリポジトリのクローン後に
`selection100.json`の100件について`body_parser.py`で再生成すればよい（Day 2以降で対応）。

---

## 1. このプロジェクトの原則（すべての判断の土台）

- **上書きしない** — 本文には触れない。案内文は本文の代わりではなく、入口
- **序列をつけない** — 「傑作」「名作」「代表作」等の評価語を使わない
- **寄生しない** — Wikipedia・文学事典・既存の解説を参照しない。本文だけを見る
- **書けないものは、書けないと言う** — もっともらしい嘘より空欄の方がよい
- **測れないものは、測れないと言う** — 検証していないことを「検証済み」と書かない

最後の2つが最重要。都合のよい条件下でだけ動く検証を、動作の証明として報告しない。

---

## 2. 【最重要】お金の話 — 毎セッション最初に確認

Pro以上の購読に含まれ追加課金なしで使えるが、罠が一つある：

> `ANTHROPIC_API_KEY` 環境変数が設定されていると、購読ではなくAPI従量課金になる。

**セッション開始時に毎回 `/status` を実行し、サブスクリプション認証を目視確認すること。**
API キー経由になっていたら作業を止めて運営者に報告する。

利用枠はclaude.aiチャットと共有。枠を使い切ったらその日は終わり（自然な日次区切り）。

---

## 3. 完成済み成果物の扱い

| ファイル | 中身 | 扱い |
|---|---|---|
| `index.json` | 全17,456作品の計測値・はじまりの一文 | **読むだけ。絶対に変更しない** |
| `authors.json` | 著者1,018人の集約 | 読むだけ |
| `under200_ids.json` | 本文200字未満895件のID | 除外リストとして使う |
| `body_parser.py` / `build_index.py` / `finalize.py` | 第一段階のパイプラインコード | 復旧済み。再利用する |
| `gates.py` / `label_vocab.py` | 品質ゲート・ラベル語彙（第2便版） | 復旧済み・動作確認済み。再利用する |
| `drafts_batch1.json` | 手書き97篇の案内文（ベンチマーク対照群） | 復旧済み（2版あった。採用理由は上記参照） |
| `selection100.json` / `labels_pass1,2.json` | 100篇の選定・ラベル2回分 | 復旧済み |
| `fulltext_100.json` | 100篇の本文全文 | **まだ見つからない。Day2以降に再生成** |

`index.json`の「計測値」「はじまりの一文」は第一段階の確定成果。第二段階では一切変更しない。

---

## 4. これまでに判明した罠（同じ穴に落ちないための地図）

### データ取得
- `--filter=blob:none` のまま全件処理しない。ネットワーク取得が毎回発生する。sparse-checkout（coneモード）＋ `--depth 1`
- 本文ファイル名は `{作品ID}_{ファイルID}.html` だが `{作品ID}.html` という別パターンも実在（見落とすと誤って取得不能扱いになる）
- 文字コードは Shift_JIS

### 本文パース
- 本文の終端は `<div class="bibliographical_information">`。**`bibliography_information`ではない**
- カギ括弧の深さは行ごとにリセットする（文書全体で追跡すると複数段落の会話で破綻する）

### 判定ロジック
- 旧仮名＝文語ではない（独立した軸）
- 会話文比率は戯曲では機能しない（「」を使わないため）
- 閾値を1作品だけで「検証済み」としない

### 案内文
- 固有名詞は本文の表記のまま（豐太郎／淺野／亜米利加、等。現代表記に直さない）
- 本文にないことを具体化しない（「わが國」を「日本」と書かない）
- 本文が短すぎると案内文は書けない → 200字未満は対象外

### ゲート
- 自己申告に依存するゲートはゲートではない。機械抽出（janome）に一本化
- 誤検知を消すために語彙リストへ語を追加しない。除外は「形の性質」で行う
- 文末集計は表層4文字で数えない（活用形の違いを別物として数えると偏りが過小評価される）

---

## 5. ゲートの仕様（`gates.py` 再構築の指針）

| 層 | 内容 | 実装方針 |
|---|---|---|
| 第三層 | 固有名詞が本文に実在するか | janome形態素解析で固有名詞抽出＋数詞除外。自己申告は使わない |
| 第四層 | 非流通語（古語）混入なし | ブラックリスト方式 |
| 第五層 | 60〜100字／評価語なし／結末示唆なし | 機械チェック |

禁止表現（GUIDE_TEXT_SPEC.md 4-3）：
不朽の名作／珠玉の一篇／心温まる／考えさせられる／胸を打つ／色褪せない／今なお／傑作／必読／
〜な人におすすめ／読者に問いかける／深く突き刺さる／人間の本質／普遍的なテーマ

---

## 6. 確定している設計判断

### 生成対象は16,203件
```
全作品 17,456件 − 200字未満 895件 − 表記違い統合 358件 = 16,203件
```
統合条件：同一著者・同一題名・文字数±10%以内・文字遣いが互いに異なる352組。
文字遣いまで同じ23組は統合しない（別作品の可能性）。`index.json`のエントリ自体は統合しない。

### 入力は本文の先頭4,000字のみ
結末を見せない構造的対策（「書くな」ではなく「見せない」）。

### 案内文の型は2つ
| 対象 | 型 |
|---|---|
| 物語 | 誰が・どこで・何をする話か |
| 随筆・評論 | 何を手がかりに・何について、どう論じているか |

### 「育つ庭」モデル
全16,203件の完成は公開条件ではない。生成順序はアクセスランキング順（読まれる作品から）。
UI側の規律（申し送り事項）：
1. 案内文の有無で並べ替えない
2. 「準備中」ラベルを出さない
3. 案内文がない作品も同じ視覚的重みで表示する

---

## 7. 較正週にやること（現状のギャップを踏まえた順序）

**ベンチマークデータが見つかるまでの間にできること：**
1. `/status` で認証確認（毎回）
2. `body_parser.py` / `gates.py` / `label_vocab.py` を仕様から再構築し、動作確認
3. 軽量版リポジトリ `aozorabunko/aozorabunko_text` の調査（本文XHTMLが揃うか、更新追随しているか）
4. 本家クローン戦略の確認（sparse-checkout, depth 1）とディスク実測

**ベンチマークデータが揃ってから：**
5. 2,000字短縮検証（`fulltext_100.json` + `labels_pass1.json`必要）
   合格線：形式0.90／主題0.65／読後感0.60／時代の質感0.90（いずれも以上）
6. N∈{3,8,15}のベンチマーク（`drafts_batch1.json`必要）
   合格線：固有名詞通過率95%以上／手書きとのJaccard0.6以上／字数規格100%／型一致90%以上／文末偏り各20%未満
7. 実測スループット確定 → 完了予定日を算出 → 運営者が第3便仕様書を改訂

**約束ではなく測定で進む。全項目を満たす最大のNを採用。満たすNがなければプロンプト側の問題として報告する。**

---

## 8. 本番運用（較正が終わってから）

毎日の型：
1. `/status` で認証確認
2. `state/checkpoint.jsonl` を読み、優先順リストの未処理IDを取得
3. N件ずつ処理：本文先頭4,000字を読む→案内文＋4軸ラベルをJSON化→`gates.py`にかける
   →不合格なら修正（上限3回）→3回落ちたら`案内文なし理由`を確定して次へ
4. 1件ごとにcheckpointへ追記（クラッシュ安全）
5. 枠が尽きたら進捗サマリ（済/残/推定完了日）を出して終了

出力形式：
```json
{
  "作品ID": "127",
  "案内文": "夜、羅生門の下で雨宿りする男。...",
  "案内文なし理由": null,
  "形式": ["小説"],
  "主題": ["貧困", "死", "身分・階級"],
  "読後感": ["不穏", "張りつめた"],
  "時代の質感": ["平安"]
}
```

`案内文なし理由`を必ず使えるようにしておく。書かせない逃げ道がないと必ず何か書いてしまう。

## 人手検品（第六層）— 運営者の仕事
層化抽出で200〜400件。判定ははい／いいえ／判断保留の三択のみ。
厚く見る層：随筆・評論／文語・旧字旧仮名／長大な作品／ゲートを2回以上落ちて通った作品（全件）。

---

## 9. まだ決まっていないこと（勝手に決めない）

1. 運営者のプラン（Pro以上と申告あり）
2. 実スループット — 較正で確定
3. 本番のバッチサイズN — ベンチマークで確定
4. Pass A（ラベル先行）の採否 — 2,000字検証の結果次第
5. 軽量版リポジトリの採否 — 調査次第
6. 読後感ラベルを残すか落とすか — 保留中
7. 「淡々」と「端正」の統合 — 検討中

---

## 10. 最後に

検証していないことを検証済みと言わない。破れなかったという報告より、破れ方を見つけた報告の方が価値がある。急がない。
