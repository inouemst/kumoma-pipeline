# 較正週 報告書

> 実施日：2026-07-24 セッション1回分。CLAUDE.md 第7章の較正週手順に従い、Claude Code (Sonnet 5) が実施。
> **原則の確認**：検証していないことを検証済みと書かない。都合のよい条件下だけで動く結果を、動作の証明として報告しない。破れなかった報告より、破れ方を見つけた報告を優先する。

---

## 0. 結論だけ先に

| 項目 | 結果 |
|---|---|
| 軽量版リポジトリ | **不採用**。正しい所在は `aozorahack/aozorabunko_text`（引き継ぎ書の `aozorabunko/aozorabunko_text` は誤り）。本文がXHTMLでなくプレーンテキストのため、既存`body_parser.py`をそのまま使えない。本家をsparse-checkoutで使用 |
| 2,000字Pass A | **不採用**。4軸すべてが合格線未達（詳細は2章） |
| N∈{3,8,15}ベンチマーク | **どのNも全項目を満たさなかった**。第三層通過率・固有名詞Jaccardが3つとも未達。バッチサイズの問題というより、プロンプト側で対処すべき欠陥パターンを複数発見した（詳細は3章） |
| 本番移行 | **時期尚早**。3章の欠陥パターンに対処してから再ベンチマークを推奨 |
| スループット | 参考値：N=8/15で97件処理に約42〜44分（実測）。16,203件は粗い試算で1〜4ヶ月（幅が大きい理由は5章） |

---

## 1. 軽量版リポジトリ調査・クローン戦略

### 1-1. `aozorabunko/aozorabunko_text` は存在しない

引き継ぎ書・CLAUDE.mdに記載の `aozorabunko/aozorabunko_text` へ `git ls-remote` したところ、認証プロンプト（実質404相当）が返った。WebSearchで確認したところ、実在するリポジトリは組織名が異なる **`aozorahack/aozorabunko_text`** だった。引き継ぎ書の組織名表記が誤っていたと判断する。

### 1-2. 中身を確認した結果、不採用と判断

`aozorahack/aozorabunko_text` のREADMEを実際に読んだ結果：

- 本文は **プレーンテキスト（青空文庫形式の.txt）のみ**。本家の `_ruby_*.zip` を展開したtxtファイルを集めたもので、**XHTML（`<div class="main_text">` 等のカード形式）は含まれない**
- 現行の `body_parser.py` はXHTMLの構造（`main_text` div、`bibliographical_information` div 等）に依存して書かれている。このリポジトリを使うには本文パーサを別途書き起こす必要があり、「復旧済みコードを再利用する」という較正週の前提から外れる
- 更新頻度（CircleCI日次）自体は問題なさそうだったが、形式の非互換性だけで不採用の判断材料として十分と判断した

**結論：本家 `aozorabunko/aozorabunko` を使う。** ディスク節約より、検証済みパーサの再利用を優先した。

### 1-3. 本家クローンの実測

- `git clone --depth 1 --filter=blob:none --sparse` → `git sparse-checkout init --cone` → 対象100作品が属する52著者ディレクトリ（`cards/000005` 等）を `git sparse-checkout set` で指定
- **`--filter=blob:none` のまま全件処理しない**（CLAUDE.md 4-1節の教訓）という指示は守った。cone modeのsparse-checkoutで必要なディレクトリだけをまとめて取得している
- **実測ディスク使用量：合計 約1.1GB**（本文ファイル741MB＋`.git` 348MB）。52著者分のディレクトリで29,344ファイル、100作品分のためだけにこの量になった

**新たに判明した点（提案・未実施）**：cone modeのsparse-checkoutは**著者ディレクトリ単位**でしか絞れず、多作の著者（例：66MBの1著者ディレクトリ）は不要な作品のzip・htmlも丸ごと落ちてくる。全16,203件（≒ほぼ全著者）を本番で処理する場合、この方式のままだと数十GB規模になりうる。non-cone（パターン指定）のsparse-checkoutで個別ファイルパスを直接指定すれば大幅に削減できる可能性があるが、**今回は検証していない**。本番クローン戦略を決める前に、この点を実測で確認することを提案する。

---

## 2. 2,000字短縮ラベル検証（Pass A可否判定）

`selection100.json` の100作品について、`fulltext_100_full.json`（後述）の冒頭2,000字を使い、`label_vocab.py` の閉じた語彙のみで4軸ラベルを再付与した（10グループに分けて並列に実施）。`labels_pass1.json`（既存の100篇ラベル、4000字入力での結果）とJaccard類似度で突き合わせた。

| 軸 | Jaccard平均（2,000字） | 合格線 | 判定 |
|---|---|---|---|
| 形式 | 0.890 | 0.90以上 | **NG**（僅差） |
| 主題 | 0.562 | 0.65以上 | NG |
| 読後感 | 0.460 | 0.60以上 | NG |
| 時代の質感 | 0.620 | 0.90以上 | NG |

**4軸すべてが合格線未達。Pass A（ラベル先行生成）は不採用と明記する。**

### 留意点（正直に書く）

- この比較は「同一セッション内の2回試行」ではなく、「今回とは別セッションで作られた `labels_pass1.json` 対 今回のラベル」の比較である。文字数削減の影響と、ラベル付け手（セッション間差）の影響が混ざっている可能性があり、完全には分離できていない
- 特に時代の質感は、第2便報告書（PHASE2_DELIVERY2_REPORT.md）が「単一セッション内の『独立2回』には限界があり、100%という数字はむしろ疑うべき」と既に指摘していた軸。今回は別セッションでの比較なので、その意味では今回の0.620の方が実態に近い可能性がある
- とはいえ、4軸中4軸が未達という結果は、文字数削減単体の影響と考えるのが自然。**2,000字への圧縮は素直に情報を失わせている**という解釈を採用する

結果は `label_2000char_test.json` として保存済み。

---

## 3. N∈{3, 8, 15} ベンチマーク

`drafts_batch1.json` の手書き97篇について、本文冒頭4,000字を入力に、Claude自身が案内文＋4軸ラベルを生成した。バッチサイズN（＝1回の生成呼び出しに何作品分の本文をまとめて渡すか）を3・8・15の3通りで、97篇全件をそれぞれ生成した（計291生成）。

### 3-1. 測定結果

| 項目 | 合格線 | N=3 | N=8 | N=15 |
|---|---|---|---|---|
| 第三層（固有名詞）通過率 | 95%以上 | 91.8%（89/97） | 80.4%（78/97） | 90.7%（88/97） |
| 固有名詞Jaccard平均（対 手書き版） | 0.6以上 | 0.506 | 0.477 | 0.507 |
| 字数規格（60〜100字）達成率 | 100% | 100%（97/97） | 99.0%（96/97） | 100%（97/97） |
| 型（小説型/随筆型）一致率※ | 90%以上 | 98.9% | 96.9% | 96.9% |
| 文末の偏り（形式名詞軸・用言軸） | 各20%未満 | OK（超過なし） | OK（超過なし） | OK（超過なし） |
| 総合ゲート通過率（全層AND） | — | 84.5% | 73.2% | 85.6% |

※型一致率は `labels_pass1.json` の「形式」ラベルを「物語型」（小説・童話・戯曲・詩・脚本）と「随筆型」（随筆・評論・紀行文・書評・追悼文・論考）の2群に括り、生成側の形式ラベルと突き合わせた（drafts_batch1.jsonに形式ラベルがないため、既存の第2便ラベルを参照点とした）。

**結論：どのNも「第三層95%以上」「Jaccard 0.6以上」を満たさなかった。** 5項目のうち3項目（字数・型一致・文末偏り）は全N合格だが、固有名詞まわりの2項目が全滅している。**指示書の原則どおり「Nを下げても解決しない＝プロンプト側の問題」として報告する。**

なお、N=8がN=3・N=15より一貫して悪い（第三層80.4%、字数99.0%、Jaccard最低）。単調な「小さいNほど良い」という単純な関係にはなっておらず、N=8だけ悪い理由は今回の1回の試行では特定できていない。**サンプル数（各N・97件が1回ずつ）では、ノイズの可能性を否定できない。**

### 3-2. 固有名詞まわりの失敗を手作業で分類した（36件、N=3/8/15合算）

ゲートの「不在語」を1件ずつ本文と突き合わせ、以下の3種に分類した。

**(A) 本物の欠陥（本文にない事実の追加・表記の現代化）：4件**
- `389`（たけくらべ）：「吉原」を使用 → 本文には現れない（大音寺前としか書かれていない）。**CLAUDE.md 4-4節に記録された、この作品そのものの既知の罠が、今回も同じ形で再発した。**
- `388`：「安兵衛」を使用 → 本文の表記は「安兵衞」（旧字）。豐太郎／淺野と同種の現代化ミス
- `3104`：「ニーチェ」を使用 → 本文の表記は「ニイチェ」（旧仮名カタカナ）。同上のパターンが人名の外来語表記でも発生することを確認した
- `56654`：「ロシア」と明言 → 本文（冒頭4,000字）に国名の明示はない。登場人物名から類推できる範囲だが、本文に書かれていない具体化にあたる

**(B) 著者名・作品名の言及（メタデータ由来、境界が曖昧）：6件**
`47087`（幸田露伴）、`47249`（国枝史郎）、`2680`（夏目漱石）、`2800`（宮本百合子）、`49552`（岡本綺堂）、`59522`（グリム）
→ プロンプトには本文だけでなく `作品名`・`著者` も渡している。随筆・評論では「〜が回想する随筆」のように著者名を書く自然な言い回しになりやすいが、著者名は本文中に出てこないため第三層は必ず弾く。**これは生成の欠陥というより、ゲートが「本文のみ」を根拠にする設計と、生成時に著者名も見せている設計の間の境界の曖昧さ**である。運営者の判断を仰ぎたい点：著者名の言及を禁止するか、第三層の照合対象に著者名・作品名も含めるか。

**(C) 抽出器の誤検知（言い換え・複合語の生成に伴うもの、実害なし）：残り約26件**
例：`楼上`／`門上`（本文は「楼へ上る」）、`峠道`、`繁著`、`父十倉奥造`（隣接語の融合）、`實態`、`丸石`、`贋刀`、`ユーモラス`／`テンポ`／`スリル`（文体を形容する一般的カタカナ語）等。
→ 事実として誤っているわけではなく、案内文が本文の表現をそのまま抜き出さずに言い換えた結果、janomeの機械抽出が「固有名詞候補」として誤って拾ってしまうケース。CLAUDE.md 4-5節が既に指摘する「誤検知を消すために語彙リストへ語を追加しない」という原則を守り、**COMMON_KATAKANAへの追加は提案しない**（いたちごっこになるため）。構造的な除外規則を検討したが、今回のセッションでは有効な「形の性質」を見つけられなかった。この点は正直に「未解決」と書く。

**この分類の意味**：ゲートが機械的に測る「第三層通過率」は91%前後だが、**本当に危険な欠陥（A）はそのうち36件中4件、全291生成中では約1.4%**に留まる可能性がある。とはいえ(A)の4件は軽視できない——うち1件（389）は過去に一度発見され記録された罠そのものが、対策後も再発した事実であり、「プロンプトに明記した個別の罠は防げても、同種の罠が別の固有名詞・別の外来語で繰り返し起きる」ことを示している。**これは較正週で見つけるべき、まさにその種の欠陥である。**

### 3-3. Jaccard 0.6未達についての留保

`fulltext_100.json`（4,000字版）を再生成する過程で、`drafts_batch1.json` の手書き案内文の少なくとも1件（389・たけくらべの「美登利」）が、本文の4,000字境界のわずか31字先（4,031字目）に初出する固有名詞を使っていることを確認した（4章参照）。つまり**手書きベンチマーク自体が、必ずしも「冒頭4,000字だけ」という制約の下で書かれたとは限らない**。これは生成側のJaccardスコアを構造的に不利にする要因になりうるが、97件中で確認できたのは1件のみであり、0.48〜0.51という低い平均値の主因とは考えにくい。**素直に見れば、独立した書き手（人間とLLM）が同じ素材から異なる固有名詞を選んで圧縮した結果と解釈するのが妥当。** Jaccard 0.6という合格線が、この種の創造的圧縮タスクに対して妥当な基準だったかどうかは、運営者の再検討を提案する。

---

## 4. fulltext_100.json の再構築と、新たに見つかった設計上の緊張関係

### 4-1. やったこと

`body_parser.py` を使い、クローンした本家リポジトリから該当100作品の本文XHTMLを取得し、以下2種類のJSONを作った。

- **`fulltext_100.json`**：本文冒頭4,000字（今回の指示通りのファイル名・内容。**生成時の入力**として使う）
- **`fulltext_100_full.json`**（新規）：本文全文（**ゲート照合の正解データ**として使う）

### 4-2. なぜ2つに分けたか（gates.pyの既存自己テストで発覚した不整合）

`gates.py` の `__main__` には「たけくらべに偽の『吉原』を混入させても第三層で弾かれる」「正しい『大音寺前』は通る」という自己テストが最初から書かれている。これを4,000字版の `fulltext_100.json` に対して実行したところ、**正しいはずの案内文（手書き版そのもの）が誤ってNG判定になった**。

原因を追ったところ、この正しい案内文が使う固有名詞「美登利」は、たけくらべの本文の**4,031字目**（4,000字の境界からわずか31字先）に初出することが判明した。つまり：

- 生成時の入力を4,000字に絞るのは「結末を見せない」ための設計（6.2節）で、これは維持すべき
- しかし**ゲートが「本文に実在するか」を確認する際の正解データまで4,000字に絞ると、境界のすぐ先にある正当な固有名詞を偽陽性で弾いてしまう**

この2つの用途（生成入力／ゲート正解データ）は本来別物であり、`fulltext_100.json`という1つの名前・1つの中身で兼用していたことが、今回の再構築で初めて表面化した不整合だと考える。**gates.pyのロジック自体は変更していない**（指示通り）。渡すデータを使い分けただけである。今回のベンチマーク測定はすべて `fulltext_100_full.json`（全文）を正解データとして使っている。

**運営者への確認事項**：`fulltext_100.json`は今回の指示通り4,000字版として保存したが、本番のゲート実装でも同様に「生成入力用（4,000字）」と「ゲート正解データ用（全文）」を分けて持つ設計にすべきだと考える。この判断は状況証拠（gates.pyの自己テストとの矛盾）によるもので、最終確認は運営者に委ねたい。

---

## 5. スループットの粗い見積もり（1回のセッションのみに基づく）

較正ベンチマーク（N=3/8/15、計291生成）を、Agentツールで最大20並列のバックグラウンド実行を使って処理した。ただし**本番の日次運用（CLAUDE.md 8章）は1つの会話セッションが順に処理する想定であり、20並列のサブエージェントは使わない**。そのため、並列実行にかかった総時間ではなく、**各バッチの所要時間の単純合計（＝1セッションが順番に同じ作業をした場合の時間）**を参考値とする。

| N | バッチ数 | 所要時間合計（実測） | 97件あたりの速度 |
|---|---|---|---|
| 3 | 33 | 約64.4分 | 約90件/時間 |
| 8 | 13 | 約43.6分 | 約134件/時間 |
| 15 | 7 | 約41.7分 | 約140件/時間 |

**この数字を鵜呑みにしないための注意**：
- 1回のセッション・1回の試行に基づく値であり、確定値ではない
- 本番はゲート不合格時の再試行（上限3回）・checkpoint書き込み・1日の利用枠終了判定が加わるため、これより遅くなる
- 3章の通り、どのNも品質基準を満たしていないため、そもそも「どのNで本番運用するか」自体が未確定

**それでも粗い試算をするなら**：16,203件 ÷ 90〜140件/時間 ≒ 116〜180時間。1日の生産的な作業時間を仮に2〜4時間とすると（**これも未検証の仮定**）、29〜90日、すなわち**約1ヶ月〜3ヶ月**。引き継ぎ書にあった「Proで約2ヶ月」という既存推定と大きくは矛盾しないが、これも実測に基づく確定予定日ではない。

**実測できなかったこと**：ゲート不合格時の実際の修正・再試行にかかる時間、1日あたりの利用枠の実際の上限、checkpoint運用込みの実運用速度。これらは本番の最初の数日で改めて実測する必要がある。

---

## 6. つまずいた点・環境上の制約

1. **GitHub API経由のリポジトリアクセスが `inouemst/kumoma-pipeline` にスコープされており**、`aozorabunko/aozorabunko` 等の外部リポジトリには `add_repo` できなかった（"cross-tier adds are not supported"）。そのため素のgitプロトコル（`git clone`/`git ls-remote`、GitHub API を経由しない）でアクセスした。`api.github.com` 経由は403で弾かれるが、`github.com` への生のgit通信は許可されていた
2. **サブエージェントの同時実行数上限が20**だった（`CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS`）。33バッチ（N=3）を一度に投げたところ超過分がエラーになり、完了を待って段階的に投入する運用に切り替えた
3. **`janome`が未インストールだった**（CLAUDE.mdには「実行済み」とあったが、今回のセッションでは入っていなかった）。`pip install janome`で解決
4. **コミットの著者情報（git config）がリポジトリの期待値と一致していない**という stop hook の指摘を受けたが、`git config`の変更は指示上禁止されているため、**対応せず運営者の判断を仰ぐ**（7章参照）
5. `/status` によるサブスクリプション認証確認は、この実行環境がClaude Code on the webのマネージド環境であり、CLIの `/status` 相当の確認手段が本セッションからは直接見えなかった。**認証方式（サブスクリプション課金かAPI従量課金か）を目視確認できていない。** これは「検証していないことを検証済みと書かない」という原則に照らし、明記しておく

---

## 7. 未対応のまま残した事項（運営者の判断が必要）

- **コミット著者の不一致**：`git config user.email/user.name` の変更、および直前コミットの `--amend --reset-author` を stop hook から求められたが、「git configは変更しない」「amendではなく新規コミットを基本とする」という運用方針と衝突するため、今回は実施していない。対応するかどうかは運営者の判断を仰ぎたい
- **著者名・作品名のメタデータ境界**（3-2節B）：生成時に著者名を見せているのに、ゲートは本文だけを正解データとする設計の是非
- **カタカナ抽出器の構造的な誤検知**（3-2節C）：語彙追加ではない構造的解決策を、今回のセッションでは見つけられなかった
- **cone modeのsparse-checkoutが著者単位でしか絞れない点**：本番の全件クローンでディスクがどこまで膨らむか、実測が必要

---

## 8. 提案（実行はしていない）

- 生成プロンプトに「著者名・作品名を案内文に書かない」旨を明記し、3-2節(B)の6件を構造的に防ぐ
- 「本文の表記をそのまま使う」という指示に、豐太郎／淺野以外の具体例（旧仮名カタカナの人名・古い漢字体の人名等、今回見つかった「ニイチェ」「安兵衞」のような実例）を追加し、汎化を試みる。ただし、これは特定の罠を塞ぐ対症療法である点は認識しておく
- fulltext_100.json（4,000字・生成入力用）と fulltext_100_full.json（全文・ゲート正解データ用）の分離を、本番のパイプライン設計にも反映する
- N=8が他の2つより一貫して悪かった点について、追加の試行（別セッション・別日）で再現するかを確認する

---

## 9. 保存した成果物

- `fulltext_100.json`（4,000字版、生成入力用）
- `fulltext_100_full.json`（全文版、ゲート正解データ用、新規）
- `label_2000char_test.json`
- `benchmark_n3.json` / `benchmark_n8.json` / `benchmark_n15.json`
- `CALIBRATION_REPORT.md`（本書）

以上をもって較正週の実施結果とする。**本番の16,203件生成には、3章で見つかった固有名詞まわりの欠陥パターンへの対処が済むまで着手しないことを推奨する。**
