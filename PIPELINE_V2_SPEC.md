# 本番パイプラインへの新ルール適用 — 実装仕様書

> 2026-08-12 作成（Opus）。実装担当（Sonnet）がそのまま作業できる粒度で書いてある。
> 前提文書：`GUIDE_TEXT_SPEC_V2.md`（仕様の本体）。**先に読むこと。**
> 本書は同文書 7章「適用順序」の 3〜5（切り出しロジックの変更とパイロット）に対応する。

---

## 0. なぜこれが最優先か

遡及修正（第1〜3段階、464件）はすべて完了・公開したが、**本番の抽出スクリプトは
まだ旧ルールのままである。**

```python
# extract_batch18_fulltext.py（バッチ19のテンプレートになるファイル）:54
fulltext_4000[wid] = full_text[:4000]        # ← 旧ルール
```

**このままバッチ19を回すと、464件かけて直した問題を新規作品で作り直すことになる。**
本作業を終えるまで、バッチ19以降に着手してはならない。

---

## 1. 【最重要・新規判明】非物語系は新ルール下で一度も検証されていない

### 1-1. 何が抜けているか

遡及修正（第1〜3段階）は**物語系（小説・童話・戯曲・脚本）だけを対象**にしてきた。
理由は「随筆・詩・評論には筋がないのでネタバレの概念が当たらない」という判断で、
これ自体は妥当だった。

しかし**新ルールの影響は「ネタバレ防止」だけではない。入力量が減ることそのもの**である。

| | 件数 | 全体比 | 新ルール下での検証 |
|---|---|---|---|
| 物語系 | 1,145件 | 65.6% | **済**（第1〜3段階で464件、うち391件を実際に書き直し） |
| 非物語系 | 601件 | 34.4% | **未（0件）** |

**次のバッチでは、およそ3件に1件が非物語系になる。** その挙動が完全に未検証のまま
本番投入することになる。

### 1-2. 具体的に何が起きうるか

`GUIDE_TEXT_SPEC_V2.md` 6-1節が挙げていた、未検証のまま残っているリスク：

> 随筆・評論では、論じている主題が本文の後半で明かされる場合、25%の入力では
> 主題を取り違える可能性がある。

随筆の案内文の型は「**何を手がかりに・何について**、どう論じているか」である。
25%しか見えないと「手がかり」（冒頭の情景・エピソード）は取れても、
「何について」（論の主題）に届かない恐れがある。

これは物語系のネタバレとは**逆向きの失敗**（書きすぎではなく、的外れ）であり、
第1〜3段階の経験がそのまま当てはまらない。

### 1-3. 非物語系のうち、影響を受ける件数

| 文字数帯 | 新ルールでの扱い | 非物語系の件数 |
|---|---|---|
| 16,000字以上 | 変化なし（4,000字上限が効く） | 約139件 |
| 1,600〜16,000字 | **入力が減る**（25%へ） | 376件 |
| 1,600字未満 | **B型へ移行** | 86件 |

とくに **1,600字未満の86件がB型に落ちる点は概念的な検討が要る**（4章で扱う）。

---

## 2. 検討したが不採用にした案（同じ検討を繰り返さないための記録）

### 2-1. 「非物語系だけ切り詰めない」案 — 不採用

随筆には隠すべき結末がないのだから、非物語系は従来どおり4,000字を渡せばよい、
という案を検討した。形式ラベルは生成時にしか決まらないが、`index.json` の
`計測値.会話文比率` は抽出時点で使えるため、これで判別できないか実測した。

| | 会話文比率の中央値 |
|---|---|
| 物語系（1,145件） | 0.218 |
| 非物語系（600件） | 0.018 |

分離はある程度きくが、**判別器としては使えない。**

| 閾値 | 物語系の検出率 | 見逃す物語系 |
|---|---|---|
| 0.02 | 87.2% | **約147件（12.8%）** |
| 0.05 | 82.6% | 約199件 |
| 0.15 | 65.1% | 約400件 |

**致命的なのは見逃し（false negative）である。** 物語系なのに非物語系と誤判定された作品は
切り詰められず、ネタバレ risk がそのまま残る。最も緩い閾値でも物語系の12.8%を取りこぼす。
**464件かけて直した問題を、147件規模で作り直すことになるため不採用。**

**→ 一律25%を維持する。** `GUIDE_TEXT_SPEC_V2.md` 3-1節の「形式で切り分けない」という
結論は正しかった（同節は鶏と卵の議論だけを根拠にしていたが、今回それを実測で裏づけた）。

---

## 3. 変更1：切り出しロジック

### 3-1. 変更箇所

次のバッチ用スクリプトを作る際、`extract_batch18_fulltext.py` をコピーしたうえで
以下の1箇所を必ず書き換える。

```python
# 旧（54行目付近）
fulltext_4000[wid] = full_text[:4000]

# 新
n = len(full_text)
cut = min(4000, max(300, int(n * 0.25)))
fulltext_4000[wid] = full_text[:cut]
```

### 3-2. ファイル名について

`batchN_fulltext_4000.json` は実態と合わなくなるが、**リネームしないこと**
（`gate_check_batch.py` 等が参照している。`GUIDE_TEXT_SPEC_V2.md` 2-4節と同じ判断）。

### 3-3. 検算（実装後に必ず実行）

```python
import json
meta = json.load(open("batchN_meta.json", encoding="utf-8"))
inp  = json.load(open("batchN_fulltext_4000.json", encoding="utf-8"))
bad = []
for wid, m in meta.items():
    n = m["文字数"]
    expected = min(4000, max(300, int(n * 0.25)))
    actual = len(inp[wid])
    if actual != min(expected, n):
        bad.append((wid, n, expected, actual))
print("不一致:", len(bad), bad[:5])
```

---

## 4. 変更2：B型の適用範囲を明確化する

### 4-1. 問題

`GUIDE_TEXT_SPEC_V2.md` 3-1節では、B型の適用条件を「文字数1,600字未満」とし、
形式では判定しないとしている（抽出時点で形式が未確定のため）。

しかし**B型の存在理由は「短い作品で筋を追うと全体を要約してしまうから、筋を追うのをやめる」**
である。随筆・詩には**そもそも追うべき筋がない。** 1,200字の随筆に「筋を追うな」と
指示することは、指示として空振りする。

同 3-5節は「詩や随筆は元々筋を追わないため、B型移行による実質的な変化は小さい」と
述べているが、これは**未検証の見込みであり、86件が該当する。**

### 4-2. 対応方針

**B型の閾値（1,600字未満）自体は変えない。** 変えるのは生成プロンプトの書き方である。

生成エージェントへの指示に、次を加えること：

> B型の対象（1,600字未満）であっても、その作品が随筆・詩・評論など、
> もともと筋を追わない種類の文章である場合は、無理に「様式予告」「状況アイロニー」の
> 型に当てはめようとしなくてよい。**従来どおり「何を手がかりに・何について書かれた文章か」
> を、渡された入力の範囲内で書けばよい。** B型が禁じているのは「出来事の連鎖を追うこと」
> であって、「主題を述べること」ではない。

これはB型の定義を緩めるのではなく、**元々の意図を明文化するもの**である。

---

## 5. パイロット（本格適用前に必ず実施）

### 5-1. 目的

新ルール適用後の挙動を、**未検証領域である非物語系を中心に**確認する。
物語系は第1〜3段階の464件で十分に検証済みなので、パイロットの主目的ではない。

### 5-2. 対象の選び方

**既に生成済みのバッチ（1〜18）から24件を選ぶ。** 新規バッチではなく既存から選ぶのは、
**旧案内文が存在し、新旧比較ができるから**である。

配分は次のとおり（非物語系16件・物語系8件）：

| 区分 | 形式 | 文字数帯 | 件数 | 狙い |
|---|---|---|---|---|
| 非物語系 | 随筆 | 1,600字未満（B型） | 3 | 4章の懸念の実地確認 |
| 非物語系 | 随筆 | 1,600〜4,000字 | 3 | 入力が最も削られる帯 |
| 非物語系 | 随筆 | 4,000〜16,000字 | 3 | 主題が後半にある risk |
| 非物語系 | 評論 | 1,600〜16,000字 | 3 | 論の主題を掴めるか |
| 非物語系 | 詩 | 任意 | 2 | 情景型。影響は小さいはず |
| 非物語系 | 紀行文 or 書簡 | 任意 | 2 | 少数形式の確認 |
| 物語系 | 小説 | 4,000〜16,000字 | 4 | 対照群 |
| 物語系 | 小説 | 16,000字以上 | 4 | **入力が変わらない群**（挙動が変わらないことの確認） |

選定は無作為でよいが、**同一著者に偏らないこと。**

### 5-3. 手順

1. `prepare_pilot.py` を作り、選んだ24件について
   `pilot_meta.json` / `pilot_input.json`（新ルールで切り出し）/
   `pilot_fulltext_full.json` / `pilot_old_guide.json` を出力する
   （`prepare_stage3_fix.py` が流用できる。著者名は `index.json` の `著者` 欄から取ること）
2. `gate_check_pilot.py` を作る（`gate_check_stage3.py` の参照先を差し替えるだけ）
3. エージェントに、新入力だけを見て24件の案内文を書かせる
   （4-2節のB型補足を含めること。字数は60〜100字、目安63〜80字）
4. 独立検証（ゲート・著者名漏出・字数分布）
5. **新旧を並べた比較表を作る**（下記）

### 5-4. 評価の観点 — ここが本題

パイロットの合否は、ゲート通過率では決まらない。**新旧を並べて、次を人手で見る。**

| 観点 | 何を見るか | 危険信号 |
|---|---|---|
| **主題の一致** | 旧案内文と新案内文が、**同じ主題**を指しているか | 新旧で「何についての文章か」が食い違っている＝25%では主題に届いていない |
| **痩せ** | 新案内文だけを読んで、何の文章か分かるか | 「〜について書かれた随筆」のように内容が空洞化している |
| **B型の空振り** | 随筆・詩のB型対象3件が不自然になっていないか | 筋のない文章に無理に「様式予告」を当てている |
| **不変群** | 16,000字以上の4件が、旧とほぼ同等か | ここが変わっていたら切り出しロジックのバグ |

**旧案内文は「より多くの入力を見て書かれたもの」なので、主題の判定においては
参考になる正解に近い。** 新旧で主題が食い違った件数を数え、**24件中3件以上あれば
非物語系への一律適用は危険**と判断し、運営者に報告して方針を再検討する。

### 5-5. 出力

比較結果を `pilot_report.md` にまとめ、運営者に提示する。含めるもの：

- 24件それぞれの「旧案内文／新案内文／文字数／新入力字数／形式」の対照表
- 主題が食い違った件数と、その具体例
- ゲート通過率・著者名漏出・字数分布
- 実装者としての所見（本格適用してよいか、条件付きか）

---

## 6. 本格適用（パイロット合格後）

パイロットで問題がなければ、バッチ19から新ルールで運用する。
その際、`CLAUDE.md` の8章（本番運用の型）に次を追記すること。

- 入力は「本文の25%（下限300字・上限4,000字）」であること
- B型（1,600字未満）の存在と、4-2節の補足
- 「新入力の範囲内だけを根拠にする」規律（`GUIDE_TEXT_SPEC_V2.md` 2-4b節）

---

## 7. 未決事項（実装者が勝手に決めないこと）

- **パイロットで主題の食い違いが多発した場合の対処。** 選択肢としては
  (a) 非物語系だけ切り出し比率を上げる（例：40%）、(b) 非物語系は従来どおり4,000字にする
  （ただし2章のとおり形式判別が信頼できないため、生成後に形式ラベルを見て
  再生成する二段構えが必要になる）、(c) 案内文の型そのものを見直す、などがある。
  **どれも運営者の判断を要する。**
- **露出率40%未満（物語系559件）を遡及対象外のままにするか。**
  第1〜3段階を終えた現時点で、改めて運営者の確認を得ること。
- **非物語系（601件）を遡及対象に含めるか。**
  本パイロットの結果によっては、遡及の必要が出てくる可能性がある。
