# 案内文生成 仕様変更 V2 — 実装仕様書

> 2026-08-09 作成。運営者の決定を受けて確定した仕様。
> 背景・調査経緯は `SPOILER_ISSUE_BRIEF.md` を参照。
> **この文書は実装担当者がそのまま作業できる粒度で書いてある。**
> 既存の `GUIDE_TEXT_SPEC.md`（第2便版）を置き換えるものではなく、**追補**として扱う。

---

## 0. 運営者による決定事項（確定済み・議論不要）

1. **入力の切り出しを「本文の25%」に変更する。ただし上限は4,000字。**
2. **極端に短い作品にも案内文はつける。** ただし外部情報に頼らず、描写・手触りに焦点を当てた別の型を用いる。
3. **遡及調査を先に行う。** 次バッチの生成より優先する。

---

## 1. 【重要】調査で判明した事実の訂正

`SPOILER_ISSUE_BRIEF.md` では問題の範囲を「本文4,000字以下の作品」と記述したが、**これは過小評価だった。**

真の問題は作品の絶対的な長さではなく、**本文の何割がAIに渡っていたか（露出率）** である。
旧ルール `full_text[:4000]` のもとでは、露出率は作品長に反比例して跳ね上がっていた。

**旧ルールでの露出率の分布（物語系1,145件）：**

| 露出率 | 件数 | 評価 |
|---|---|---|
| 90%以上（ほぼ全文） | 169件 | 結末まで露出。最も危険 |
| 60〜90% | 143件 | 終盤まで露出 |
| 40〜60% | 152件 | 中盤以降まで露出 |
| 30〜40% | 122件 | 境界域 |
| 30%未満 | 559件 | **良好。実地確認でも問題なし** |

実地に精読して「良い案内文」と確認できた作品（愚人の毒25%、旗本退屈男21%、老中の眼鏡29%）は
いずれも露出率30%未満だった。逆に明確な破綻例（豕、露出率100%）は露出率100%だった。

**→ 露出率25%という新ルールは、経験的に良好と確認された帯域に全作品を揃える措置である。**

---

## 2. 変更1：入力切り出しの割合化

### 2-1. 変更箇所

`extract_batchN_fulltext.py`（毎バッチ、前バッチからコピーして作成している）の以下の1行。

```python
# 旧
fulltext_4000[wid] = full_text[:4000]
```

```python
# 新
n = len(full_text)
cut = min(4000, max(300, int(n * 0.25)))
fulltext_4000[wid] = full_text[:cut]
```

### 2-2. 挙動の検算（実装後に必ず確認すること）

| 本文の長さ | 切り出し量 | 露出率 | 備考 |
|---|---|---|---|
| 100,000字 | 4,000字 | 4% | 上限が効く |
| 16,000字 | 4,000字 | 25% | 上限とちょうど一致する境界 |
| 15,830字（愚人の毒） | 3,957字 | 25% | 従来とほぼ同じ。良好例を壊さない |
| 8,000字 | 2,000字 | 25% | 従来は50%露出だった。改善 |
| 1,600字 | 400字 | 25% | A型の下限 |
| 853字（豕） | 300字 | 35% | 下限300字が効く。**B型対象** |
| 250字 | 250字 | 100% | 全文だが**B型**なので筋を追わない |

### 2-3. 影響範囲

- **16,000字以上（597件相当）：変化なし。** 従来どおり4,000字。
- **1,600〜16,000字（1,031件相当）：入力が減る。** これは意図した変更である。
  従来この帯域は25%を超える露出があり、リスク域だった。
- **1,600字未満（118件相当）：B型へ移行**（次章）。

### 2-4b. 【重要】入力境界の規律 — ゲートでは検出できない問題（2026-08-09 実地検証で判明）

5件の試作で、**結末を明かしていないのに問題がある案内文**が2件見つかった（桜の樹の下には・指）。
どちらも、旧案内文が使っていた具体的な描写（「渓で見た無数の羽虫の骸」「新しい曲を弾く真似をする」）が、
**新しく渡す入力（25%切り出し）より先、本文の奥にしかない情報**だった。

これは`gates.py`の第三層（固有名詞照合）では検出できない。第三層は**全文**に対して
実在確認をするため、本文の奥にある固有名詞は「実在する」として普通に通ってしまう。

**→ 実装（生成AI・エージェント）側の規律として徹底する必要がある：**

> **案内文に書くことは、その作品に渡された新入力（`stage1_input.json`等）の範囲内だけを
> 根拠にすること。全文を検索して裏を取る、全文にある表現を借りてくる、といったことをしない。**

これはA型・B型どちらにも共通する規律であり、単に「結末を書かない」より広い制約である。
「豕」や「あじさい」のような明確な結末露出だけでなく、「桜の樹の下には」（結末の再掲に過ぎず
実質スポイラーではないが、引用した描写が入力範囲外だった）のような**軽微だが同種の逸脱**も対象になる。

### 2-4. ファイル名について

`batchN_fulltext_4000.json` という名前は実態と合わなくなるが、**リネームしないこと。**
`gate_check_batch.py` 等が参照しており、変更すると影響範囲が広がる。
名前が実態と異なる点は本仕様書に記録されているのでよしとする。

---

## 3. 変更2：B型（描写フォーカス型）の新設

### 3-1. 適用条件

**`batchN_meta.json` の `文字数` が 1,600 未満の作品。** 形式ラベル（小説／随筆等）では判定しない。
理由：形式ラベルは生成時に決まるため、抽出時点では未確定（鶏と卵の問題）。文字数なら抽出前に確定している。

該当は全体の約6.8%（118件相当。うち物語系32件、随筆・詩等86件）。

### 3-2. B型の目的

短い作品では、筋を追うと必然的に全体を要約してしまう。
そこで**筋を追うことをやめ、「どういう手触りの、どういう情景の作品か」を書く。**

### 3-3. B型の「引き」の作り方 — 2026-08-09 実地検証で確認した2つの型

169件のうち5件を人手で試作し、運営者と検討した結果、B型で読む動機を作る方法は
**少なくとも2種類あり、どちらも有効**であることを確認した。実装時はどちらを使ってもよい。

**型1：語りの様式を予告する**

「どんな出来事が起きるか」ではなく「どんな読み方をする作品か」を最後に示す。

> 例（豕・853字）：四明山、中秋の澄んだ月夜。庭で琴を弾く男のもとへ、外から女の声が届く。
> 戸を開けると若い女が立っていた——**静かな夜に会話だけで語られていく、短い怪異の話。**

末尾の「静かな夜に会話だけで語られていく、短い怪異の話」がそのまま引きになる。
運営者の評：「どういった話なのかのイメージが湧くので読みたいという引きにしっかりとなっている」。

**型2：状況のアイロニーで引く**

本人は知らないが読者には見えている構図を、結末に触れずに提示する。

> 例（指・1,317字、江戸川乱歩）：麻酔から覚めたピアニストは、右手首を失ったことをまだ知らない。
> 夜道で何者かに斬りつけられ、この病院に運びこまれた——「指は大丈夫だろうか」と尋ねる患者に、
> 医師が答え始める。

「医師が答え始める」で止め、結末（アルコール漬けの手首が動く場面）には一切触れない。
状況そのものが持つ緊張だけで引きになっている。

**どちらの型を選ぶかは、作品の性質に委ねてよい。** 静かな情景の作品は型1、
状況に構造的な緊張がある作品（本人が知らないことを読者が知っている等）は型2が
自然に出てくることが多い、というのが5件での傾向。無理にどちらかに寄せる必要はない。

### 3-3b. B型で書いてよいこと（すべて本文だけから判断できるもの）

- **舞台・情景**：どこで、いつごろの、どんな場所の話か（本文に書かれている範囲で）
- **語りの手触り**：淡々と／畳みかけるように／会話を連ねて／独白で、など
- **筆が費やされているもの**：色彩／音／天候／身体の感覚／食べ物／道具立て など、
  その作品が繰り返し描いている対象
- **文体的な特徴**：旧仮名づかい、文語、会話が大半を占める、など
- **冒頭の一場面**：ただし「静止画」として。そこから何が起きるかは書かない
- **状況そのものの緊張**（型2）：登場人物の認識と、本文に示された事実とのずれ。
  ただし「その先どうなるか」までは書かない

### 3-4. B型で書いてはいけないこと

- **出来事の連鎖**：「〜し、そして〜する」「〜すると〜だった」という形で筋を追うこと
- **冒頭の状況より先の展開**：登場人物がその後どうなるか、何が判明するか
- **外部知識に由来するすべて**：時代背景の解説、作家の文学史的位置づけ、他作品との影響関係。
  **これらは5原則の「寄生しない」に抵触するため、運営者の当初案から意図的に除外した。**
  「この作品と関連する作品」を示したい場合は、案内文に書くのではなく、
  将来実装する「隣から」機能（ラベルの重なりで算出）で扱う。
- A型と共通の禁止事項（評価語・序列語・著者名・作品名）はすべてB型にも適用される。

### 3-5. 書き換え例（実例に基づく）

**豕（853字、中国怪談の翻案）**

| | 案内文 |
|---|---|
| 旧（A型・全文露出） | 山中で琴を弾く男のもとへ、夜ごと訪れる娘。名残惜しさのあまり相手の履物を隠すと、**姿を消した娘の跡には血の筋が残っていた。** |
| 新（B型） | 四明山の秋、中秋の月あかり。庭を歩き琴を弾く男の音に、闇の向こうから声が応える。短い怪異を、会話を連ねて淡々と語る一篇。 |

旧版は終盤の伏線そのものを書いてしまっている。新版は情景と語りの手触りだけを示し、
何が起きるかには触れていない。

**古街（249字、詩）**

現行の案内文「たそがれどきに紅い灯がまたたく、寂れゆく古い街。蔦の絡む石垣や溝の音に耳を澄ませ
ながら、その街と離れがたい思いを重ねてうたう詩。」は**すでにB型の要件を満たしている。**
詩や随筆は元々筋を追わないため、B型移行による実質的な変化は小さい。

### 3-6. 字数

**A型と同じ60〜100字（目安63〜75字）を維持する。** B型だけ字数を変えると、
一覧に並んだときに見た目の違いが生じ、「なぜこの作品だけ扱いが違うのか」という
違和感につながる（「序列をつけない」原則に関わる）。

---

## 4. 変更3：遡及調査の手順

### 4-1. 優先順位

露出率が高い順に確認する。全464件（露出率40%超の物語系）を一度に見るのは非現実的なので、
段階的に進める。

| 段階 | 対象 | 件数 | 判断 |
|---|---|---|---|
| 第1段階 | 露出率90%以上・物語系 | 169件 | 最優先。ほぼ全文が渡っていた |
| 第2段階 | 露出率60〜90%・物語系 | 143件 | 第1段階の結果を見て要否判断 |
| 第3段階 | 露出率40〜60%・物語系 | 152件 | 同上 |
| 対象外 | 露出率40%未満 | — | 実地確認で良好だったため見送り |
| 対象外 | 非物語系（随筆・詩・評論等） | — | 筋がないためネタバレの概念が当たらない |

### 4-2. レビューページの拡張

`build_review_page.py` を拡張し、以下のフィルタを実装する。

- 対象を全バッチ（1〜18）に広げる（現在は引数で指定したバッチのみ）
- **露出率**（`min(4000, 文字数) / 文字数`）を計算し、各項目に表示する
- 露出率と形式（物語系か否か）で絞り込めるようにする
- 露出率の高い順に並べる

出力は `review_retro.html` として、公開サイトに `review-retro.html` で配置する
（既存の `review.html` は残す）。

### 4-3. 運営者が判定した後の扱い

要修正と判定された作品は、**再生成ではなく個別修正**を推奨する。
理由：再生成すると全97件単位のバッチ処理が必要になりコストが大きい。
問題のある作品だけを対象に、新ルール（割合切り出し＋必要ならB型）で
個別に生成し直すほうが安上がりで確実。

修正時は `production_batchN.json` の該当エントリと、
`state/checkpoint.jsonl`・`kumoma_prototype.html` の `D` 配列（`w[16]`）を
整合させること。**checkpoint.jsonlは追記専用で運用してきたため、
修正の記録方法は未確定（下記6-2参照）。**

---

## 5. 検証方法

### 5-1. パイロット（本格適用前に必ず実施）

いきなり次バッチ全97件に適用せず、**既に生成済みのバッチから短い作品20件程度を選び、
新ルールで再生成して旧版と並べて比較する。**

比較の観点：
- 結末・終盤の伏線に触れていないか
- 入力が減ったことで、案内文が痩せすぎていないか（「何の話か分からない」水準に落ちていないか）
- B型が「あらすじではないが、読む気を起こさせる」ものになっているか

### 5-2. 切り出しロジックの機械的確認

新しい `extract_batchN_fulltext.py` を実行後、以下を確認する。

```python
import json
meta = json.load(open("batchN_meta.json", encoding="utf-8"))
inp  = json.load(open("batchN_fulltext_4000.json", encoding="utf-8"))
for wid, m in meta.items():
    n = m["文字数"]
    expected = min(4000, max(300, int(n * 0.25)))
    actual = len(inp[wid])
    # 本文がexpectedより短い場合は全文になる
    assert actual == min(expected, n), (wid, n, expected, actual)
print("切り出しロジック OK")
```

### 5-3. 既存ゲートについて

`gates.py` の第五層に「結末示唆語」チェックがあるが、これは**キーワード一致であり、
「豕」のような伏線の露出は検出できなかった。** 今回の変更はゲートの強化ではなく
入力側の制御なので、ゲートには手を入れない。**ゲートが通ることは、
ネタバレしていないことの証明にはならない**点を、引き続き認識しておくこと。

---

## 6. リスクと未決事項（実装者が勝手に決めないこと）

### 6-1. 随筆・評論の案内文が痩せる可能性

割合切り出しは形式を問わず一律に適用される（形式は抽出時点で未確定のため）。
随筆・評論では、論じている主題が本文の後半で明かされる場合、25%の入力では
主題を取り違える可能性がある。

**→ パイロットで随筆を数件含めて確認すること。** 問題が出たら、
形式による切り分け（例：`index.json` の会話文比率で物語性を推定する）を再検討する。

### 6-2. 修正済み作品を checkpoint.jsonl にどう記録するか【2026-08-09 決定】

**運営者が決定：新しい行を追記し、後勝ちとする。** `由来`フィールドには`revision_stage1`
（第2・3段階の遡及修正時はそれぞれ`revision_stage2`/`revision_stage3`）を入れて区別する。

`build_next_batch.py`の`done_ids`はsetへの単純な集約のため、同一作品IDが複数行あっても
壊れないことを確認済み（同じ理由で`kumoma_prototype.html`の`D`配列も、作品IDをキーにした
辞書で上書きする`merge_batch.py`と同じロジックが使える）。

**ただし、2026-08-09時点ではまだ実際のcheckpoint.jsonl・公開サイトへの反映は行っていない。**
第1段階169件はエージェントによる修正案作成・独立検証まで完了し、`review-retro.html`に
反映して運営者が人力確認できる状態にした段階。実際のマージ・公開は確認後の別ステップとする。

### 6-3. B型の閾値1,600字の妥当性

25%が400字を下回る点として設定したが、実地検証はしていない。
パイロットの結果次第で1,200字や2,000字に調整しうる。**暫定値である。**

### 6-4. 「豕」の書き換え例は私の試案

3-5節のB型の例文は、本仕様書の作成者が書いた**試案であり、ゲートを通していない。**
実装時にそのまま採用せず、必ず `gate_check_batch.py` で検証すること。

---

## 7. 適用順序（推奨）

1. `build_review_page.py` を拡張し、遡及調査ページを生成（4-2節）
2. 運営者が第1段階（169件）を確認、修正要否を判断
3. 並行して、切り出しロジックを変更（2-1節）
4. パイロット20件で新旧比較（5-1節）
5. 問題なければ次バッチ（19）から本格適用
6. 6-2が決まり次第、遡及修正に着手

**3〜5は2の結果を待たずに進めてよい**（独立しているため）。
ただし6は6-2の決着が前提。
