# お便りフォームの作り方（運営者の作業）

`otayori.html` は完成していますが、**フォーム本体はまだ繋がっていません。**
いまは「お便りフォームは準備中です」と表示される状態で、リンク切れにはなりません。

以下の作業をすると繋がります。所要15分ほど、費用はかかりません。

---

## 1. フォームを作る

1. <https://forms.google.com> を開く（普段のGoogleアカウントでOK）
2. 「空白のフォーム」を選ぶ
3. タイトルを **「雲間へのお便り」** にする
4. 説明欄に、たとえば次のように書く

> 案内文の誤りのご指摘や、使い勝手のご提案をお寄せください。
> お名前・連絡先は任意です。すべて読みますが、返信はお約束できません。

---

## 2. 設問を6つ作る

**順番と種類が大事です。**とくに2番目と3番目は、あとで自動入力に使います。

| # | 質問 | 種類 | 必須 |
|---|---|---|---|
| 1 | どんなお便りですか | ラジオボタン | ● |
| 2 | 作品ID | 記述式（短文） | — |
| 3 | 作品名 | 記述式（短文） | — |
| 4 | お便りの内容 | 段落 | ● |
| 5 | お名前（任意） | 記述式（短文） | — |
| 6 | 返信先（任意） | 記述式（短文） | — |

**1番の選択肢**はこの4つにしてください。

- 案内文が本文と食い違っている
- こうだったら探しやすい
- 表示がおかしい
- そのほか

**2番・3番**には、説明として「サイトから自動で入ります。空欄のままで大丈夫です」と添えておくと親切です。

**5番・6番**には「書かなくてかまいません。サイトに載せることはありません」と添えてください。

---

## 3. 設定を確認する（ここが一番大事）

右上の**歯車マーク**を開いて、次の3つが**すべてオフ**になっていることを確かめてください。

- ❌ メールアドレスを収集する
- ❌ 回答を1回に制限する（ログインが必要になってしまいます）
- ❌ ログインを必須にする

これらがオンだと、**読者にGoogleアカウントでのログインを強いることになります。**
匿名で気軽に指摘できることが、このフォームの一番の値打ちなので、必ずオフにしてください。

---

## 4. 「事前入力したURL」を取得する

作品IDを自動で入れるための番号を調べます。

1. フォーム編集画面の右上、**点が3つ並んだボタン**を押す
2. **「事前入力したURLを取得」**を選ぶ
3. プレビューが開くので、
   - **作品ID** の欄に `AAA` と入力
   - **作品名** の欄に `BBB` と入力
4. 下の**「リンクを取得」**を押し、出てきたURLをコピーする

コピーしたURLは、だいたいこんな形をしています。

```
https://docs.google.com/forms/d/e/1FAIpQLSdXXXXXXXXXXXX/viewform?usp=pp_url&entry.123456789=AAA&entry.987654321=BBB
```

ここから**3つ**を読み取ります。

| 欲しいもの | 上の例でいうと | 読み取り方 |
|---|---|---|
| `base` | `https://docs.google.com/forms/d/e/1FAIpQLSdXXXXXXXXXXXX/viewform` | `?` より前の全部 |
| `entryWorkId` | `entry.123456789` | `=AAA` の左側 |
| `entryTitle` | `entry.987654321` | `=BBB` の左側 |

---

## 5. otayori.html に書き込む

`otayori.html` を開くと、下のほうにこう書いてある場所があります。

```js
const FORM = {
  base:        "",   // 例: "https://docs.google.com/forms/d/e/1FAIpQLSxxxxxxxx/viewform"
  entryWorkId: "",   // 例: "entry.123456789"
  entryTitle:  ""    // 例: "entry.987654321"
};
```

`""` の中に、4で読み取った3つを貼り付けます。

```js
const FORM = {
  base:        "https://docs.google.com/forms/d/e/1FAIpQLSdXXXXXXXXXXXX/viewform",
  entryWorkId: "entry.123456789",
  entryTitle:  "entry.987654321"
};
```

**引用符 `"` を消さないように**気をつけてください。ここだけ直せば、ボタンが自動で有効になります。

---

## ★ いちばん簡単な進め方

**5番の書き込みは、やらなくて大丈夫です。**

4番でコピーした**URLをそのまま私に貼ってくれれば**、こちらで読み取って書き込み、
動作確認と公開まで進めます。渡していただくのは、これ1本だけです。

```
https://docs.google.com/forms/d/e/1FAIpQLS.../viewform?usp=pp_url&entry.123456789=AAA&entry.987654321=BBB
```

このURLに個人情報は含まれません（フォームの場所と設問の番号だけです）。

---

## 6. 公開する

私のほうで `kumoma-pipeline` に書き込み、`kumoma-site` へコピーしてpushします。
数十秒で公開サイトに反映されます。

---

## 動きの確認

繋がったあとは、こう動きます。

- **作品カードの「この案内文について知らせる」を押す**
  → お便りページが開き、その作品名が表示される
  → 「お便りを書く」を押すと、**作品IDと作品名が入った状態**でフォームが開く
- **ナビの「お便り」から入る**
  → 作品の指定なしで、まっさらなフォームが開く

---

## 届いたお便りの見かた

フォーム編集画面の**「回答」**タブに溜まっていきます。
右上の表計算アイコンからスプレッドシートに繋いでおくと、一覧で見やすくなります。

案内文への指摘が届いたら、`state/checkpoint.jsonl` と `production_batchN.json` の
該当作品を直し、サイトに反映する——という流れになります。
（この直し方は、必要になったときに手順を作ります。）
